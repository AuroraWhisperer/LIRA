# LIRA 使用文档覆盖度差距审计

审计对象：`public/pages/admin/toolbox/usage-guide.html`（1291 行）
审计方式：只读。逐节提取文档知识点，对照 `public/pages/admin/**`（页面片段）、`public/js/admin/**`（前端逻辑）、`src/**`（领域/服务端文案与契约）、`public/css/**`（样式常量）。
证据格式：`文件路径:行号`。凡无法从代码确认的条目均在备注中标注「需人工确认」。

## 0. 前置事实校正（与任务描述不一致处）

| 任务描述 | 实际情况 | 证据 |
| --- | --- | --- |
| 12 个 `<section class="usage-guide-section">` | 正确，共 12 节 | `public/pages/admin/toolbox/usage-guide.html:58,93,153,206,221,319,405,485,629,776,840,891` |
| 12 条 FAQ | 实际 **14 条** `<details class="usage-guide-faq">` | `usage-guide.html:897,921,945,969,992,1016,1040,1063,1088,1113,1137,1162,1186,1210,1250`（15 个出现行，其中 1250 行所在 details 的 summary 标题在 1252 行；按 `<strong>` 标题去重后为 14 条，另见下方 §5 明细） |
| 截图 10 张 | 目录内 9 组文件、**两种格式共 19 个文件**（`.webp` + `.png` 同名对），实际画面 **9 张**：7 张 DeepSeek 后台 + 2 张安装程序 + 1 张卸载程序文件名截图 = 10 张画面（`uninstall-lira` 仅 webp） | `public/img/usage-guide/` 目录清单 |
| 「新手引导」 | 存在**两套**引导：`interactive-tour.js`（8 步气泡）与 `onboarding.html`（7 步弹窗） | `public/js/admin/interactive-tour-config.js:27-159`；`public/pages/admin/toolbox/onboarding.html:39-187` |

---

## 1. 逐节现有知识点提取

### 01 快速上手（`usage-guide.html:58-90`）
- 安装路径建议英文、不含中文或表情，示例 `D:\Apps\LIRA`、`C:\Apps\LIRA`（68-69）。
- 已安装正常可继续用原位置，更新不改安装位置（70）。
- 登录 LIRA：`「注册新账号」`（用户名+密码+注册激活码）/`「登录已有账号」`（+短效登录码）（75）。
- 连接直播间：`「点歌 → 设置」`→ 扫码登录直播账号 → 填直播间号 → 开启`「接收弹幕和礼物」`→`「保存设置」`（79）。
- 添加歌曲：`「点歌 → 导入导出」`下载模板后导入；也可在「歌库」手动添加（83）。
- 选择音乐平台：QQ 音乐/网易云在 LIRA 内登录；全民 K 歌在自有客户端登录（87）。

### 02 账号与设备（`:93-150`）
- 三类账号需分别登录：LIRA 账号、直播账号、音乐账号（98）。
- 注册：`「注册新账号」`→`「注册并进入」`（105）；用户名 2–32 位字母/数字/短横线，不支持中文，短横线不能在首尾，大写转小写，用户名用于网页歌单地址（106）。
- 换电脑：`「登录已有账号」`+原用户名密码+`「短效登录码」`；注册激活码与短效登录码均一次性（107-108）。
- `「正在为你准备直播工具」`等待；失败检查网络点`「重试」`（109）。
- 自动登录；失败点`「重试连接」`（115）；账号停用/设备授权撤销找管理员，不要删登录文件（116）。
- 直播账号三步：`「扫码登录直播账号」`→ 直播间号+`「接收弹幕和礼物」`→`「保存设置」`，异常点右上角`「刷新直播」`（123-127）。
- `「本机已登录」`只说明扫码成功（128）。
- 警告：关闭 LIRA 仍继续接收；停止需关`「接收弹幕和礼物」`并保存或退出直播账号（129）。
- 音乐平台：QQ 音乐/网易云在播放页顶部切换平台后点右上角`「登录」`；全民 K 歌只读取本机客户端播放的歌曲与逐字歌词（139-146）。

### 03 更新或重新安装（`:153-203`）
- 安装程序提示已安装过 LIRA 时保持「仅为我安装」，直接`“下一步”`（163-168，配图 `install-existing-user.webp`）。
- 安装位置不变直接`“安装”`（185）；提示「下一步」和「安装」在窗口底部同一位置（187-188，配图 `install-existing-location.webp`）。

### 04 主流程（`:206-218`）
- 5 步：打开登录 → 确认直播间已连接 → 备好歌库接点歌（观众发「点歌 歌名」）→ 选歌播放（播放页选平台、搜索、加入播放队列，底部播放器暂停/切换/进度）→ 按需开直播工具并复制链接到直播姬/OBS。

### 05 点歌页详解（`:221-316`）
- 顶部 SC 队列（付费留言）与点歌队列，可置顶/移除/查看详情（231）。
- 观众发「点歌 歌名」；歌名不完整也会尝试匹配歌库（232）。
- 点歌队列 vs 播放队列区别；`「切歌」`把第一首标为完成；切换正在播放用播放页播放器（233）。
- `「清空全部队列」`只移除点歌队列，不删歌库（234）。
- 歌库可增改删，设分类/标签/语言/是否可点；联网自动同步到同账号其他电脑；点歌队列与播放队列不同步（240）。
- 删除歌曲影响同账号其他电脑与网页歌单；`「不可点」`的歌不会被随机点歌选中（241）。
- 随机点歌条件用 `+` 连接（AND）；按语言（日语/韩语/英语/粤语/国语）、按分类（流行/R&B/说唱/摇滚/民谣/舞曲/影视原声）、按歌手、按标签（国风/小甜歌/影视OST/K-Pop 等）；示例「随机点歌 日语+流行」「随机点歌 说唱 苦情」（247-262）。
- 点歌页子标签清单（268-294）：`设置`（直播账号登录、直播间号、接收开关、暂停接收点歌、队列上限、同一观众点歌间隔秒、允许重复点歌、只允许歌库歌曲）；`点歌板`（颜色/字号/毛玻璃/发光/滚动模式与速度）；`展示板`（滚动速率、排序方式、主题配色）；`浏览器源`（集中复制全部 11 个地址，按点歌与音乐/直播互动/场景与氛围分类）；`导入导出`（XLSX/CSV/TSV/粘贴表格、下载模板、导出 Excel）；`桌面歌词设置`（字体/颜色/字号/描边/透明度 + 实时预览）。
- 云端歌单同步：登录并联网后自动同步，新电脑先用云端，云端空才上传本机（300）；`「点歌 → 导入导出 → 网页歌单同步」`→`「查看网页歌单」`（301）；`「同步到网页」`会替换云端歌库（302）；上限 5000 首；多机同时改会互相覆盖；上传失败自动重试（303）。
- 歌单页背景：支持 PNG/JPG/JPEG/WebP/GIF，单张 ≤5 MB，上传覆盖上一张，`「删除背景」`恢复默认水彩背景（309-313）。

### 06 播放页详解（`:319-402`）
- 三平台音源：顶部选 QQ 音乐/网易云音乐/全民 K 歌；前两者需分别点右上角登录；连接问题点`「检查音乐平台连接」`（329）；全民 K 歌需先开自有客户端登录（330）。
- 快捷入口：为你推荐/每日推荐/我喜欢/歌单入口；`「心动 / 电台」`快放完自动加 10 首（336）。
- 在线搜索：跟随当前平台，返回数量 1–30 可调，结果一键加入播放队列，`「清除」`开始下一次（343-344）。
- 全民 K 歌歌词同步三步：打开电脑版登录播放伴奏 → 在`「播放 → 全民 K 歌」`选 `WeSingCache` 文件夹点`「保存并检测」` → 用`「歌词时间调整」`前后微调，最多 3 秒（351-355）。
- 播放器控制栏：固定底部，封面/歌名歌手（超长跑马灯）/可拖拽进度条；上一首、下一首、播放暂停、播放模式（顺序/随机/单曲循环）；可把当前歌曲加入音乐平台歌单；顶部按钮折叠收起（361-364）。
- 播放队列：管理播放器待播音乐，与点歌队列分开；`「清空队列」`停止播放并移除待播，不删歌库与平台收藏（371-372）。
- 全屏歌词：逐字高亮，独立`「罗」`（罗马音）和`「译」`（翻译）按钮各自开关；切歌时歌词模式自动重置（378-380）。
- 桌面歌词浏览器源：`「点歌 → 桌面歌词设置」`调样式并预览，`「复制桌面歌词」`取地址；加入直播姬「浏览器」/OBS「浏览器源」后显示完整歌词、翻译、罗马音、逐字高亮与播放进度（386-391）；背景透明、尺寸由源宽高决定（394）；歌词区可独立滚动，手动查看后暂停跟随、稍后自动回到当前行（397）。

### 07 礼物页详解（`:405-482`）
- `「礼物统计」`控制本机是否统计；`「礼物提示」`只控制通知；三者（含加班机）分别设置（415）；关闭礼物统计不影响加班机，也不停止云端接收（416）。
- `「礼物提示」`开启后在 LIRA 右上角显示送礼人、礼物和金额（422）。
- `「最近礼物」`看送礼人/金额/时间；`「查看全部」`看更早记录（428）。
- 礼物历史自动更新；`「正在更新礼物记录…」`等待；断网可看已保存记录（434）；`「当前仅能查看部分历史记录」`表示不完整；加载失败点`「重新加载」`或`「重试」`（435）。
- `「清空全部记录」`永久删除本机与云端全部礼物记录，其他设备同步后也清空，无法恢复（436）。
- 月底冲刺：设月度目标金额，显示已收/还差/折合水晶球（**文档写 2333 元/个**）（443-444）；可重置本轮，礼物记录不受影响。
- 今日盲盒盈亏：按观众汇总开盒数、盒型、总成本、开出价值、净盈亏；内置盲盒类型独立配色；点观众看完整开盒记录；`「查看全部」`进盲盒分析工作区（452）。
- 盲盒分析工作区支持按观众/按盲盒类型/按开盒记录三种视图；支持筛选、排序、分页，收新礼物自动刷新；成本未知的盲盒不能按零成本估算盈利（456-459）。
- 盲盒盈亏榜投屏：自定义投屏标题、排行人数（-1 全部、0 仅汇总、1–10 对应人数）、仅显示盈利、仅显示心动盲盒；复制链接用于直播姬/OBS；画面跟随捕捉窗口自动调整（468-471）。
- 盲盒设置：常见盲盒成本与产出自动获取；需补充时添加自定义盲盒、成本和可能开出的礼物；设置同步同账号；修改只影响之后记录（478-479）。

### 08 百宝箱详解（`:485-626`）
- 概览：弹幕姬/加班机/小游戏；日历、备忘、待办；设置、性能检测、桌面更新（490）。
- 账号与安全：`「百宝箱 → 设置」`看当前账号与本机信息；忘记密码/增删设备找管理员（496）；换电脑流程（497）；自动同步项（歌库、直播账号登录状态、直播间号、点歌规则、自定义盲盒设置）；需重设项（音乐平台登录、点歌与播放队列、界面与直播画面样式；本地文件、日历、备忘、待办按机器保存）（498-499）；不要直接复制数据文件夹（500）。
- 弹幕姬：连接状态（发送账号含昵称、目标直播间含房间名、发送状态、右侧刷新按钮）（509-510）；样式选择→`「预览效果」`→`「应用到直播画面」`→把`「直播画面链接」`加入 OBS/直播姬；只预览不改变画面，应用后不用重新复制（512）；发送弹幕（手动输入、自动钓鱼/打劫、发送结果，不另设网页地址）（514-515）；点歌回复（单独控制`「随机点歌回复」`，无匹配时自动 @ 提醒）（518-520）；AI 回复（选平台、填密钥、触发关键词、回复长度、联网搜索）（522）；固定回复（签到、抽签、自定义关键词）（523）。
- AI 互动助手：触发关键词无默认值需自定义保存；未设置不触发（530）；AI 平台/密钥/模型；密钥保存后不完整显示（532）；回复规则（触发关键词→等`「已保存」`→回复长度/联网搜索/深度思考，能力以平台与模型为准）（533）；扩展能力（和风天气、高德地图）（535）；高级设置（同时处理提问数、同一观众提问间隔、每分钟上限、AI 性格与说话方式）→`「测试 AI 连接」`（538）。
- 礼物姬：`「礼物边框」`按金额门槛触发；`「滚动礼物」`循环展示今天的记录，可调行数、间隔、词条配色并复制 OBS 地址（546）；在`「礼物 → 最近礼物 → 查看全部」`选记录进导出页，设输出方式、背景、保存文件夹，预览后导出 PNG（547）。
- 小游戏：数字炸弹、五子棋、你画我猜共用`「游戏网页」`地址，按各游戏面板设置参与观众或回合后开始；你画我猜题词只在主播面板查看；转转盘用单独链接，可编辑选项和份数，保存后抽取（554-555）。
- 加班机：先设初始时长与礼物规则 → 点`「启用加班机」`→ 点`「开始」`（562）；规则可选`「直接改时间」`/`「随机抽结果」`/`「文字展板」`；时间操作支持增加、减少、乘除倍数、清零；文字展板不改时间（565）；`「按连击组」`每组结算一次，`「按具体数量」`×N 结算 N 次（568）；`「刷新在售礼物」`；设初始时长或`「重置」`恢复初始时长并暂停；`「最近结算」`保留最近 20 组（572-573）；`「直播画面」`复制投屏地址、打开实时预览，背景可透明或内置样式（576）；`「暂停」`只停倒计时，收到礼物仍可能改时间，要停止改时需关闭加班机；`「待结算」`表示等待处理（579）。
- 礼物特效：收到指定礼物在直播画面播全屏动画；输入礼物 ID 点`「播放特效」`测试；`「直播投屏」`地址固定不变，加入直播姬「浏览器」/OBS「浏览器源」，无内容时画面透明；可点`「打开预览」`（587-590）。
- 开播动画与萌时钟：开播动画设开场文案、动效、音乐，用`「显示整套特效」`控制预览与固定浏览器源；萌时钟选风格并实时预览，按面板推荐尺寸添加浏览器源；两者各用各自链接（597-599）。
- 主播工作台：日历排直播与日程、备忘记事、转待办后勾选；内容保存在当前电脑（605）。
- 性能检测：看电脑与 LIRA 的处理器、显卡、内存占用；`「电脑」`为全部程序，`「LIRA」`为本软件；点`「开始检测」`等完成（611-612）。
- 桌面更新三步：`「检查更新」`→ 需要时`「下载更新」`→`「重启并更新」`；失败从「项目主页」下载安装包；反馈问题可在`「日志目录」`找错误记录（619-623）。

### 09 配置 AI 助手（以 DeepSeek 为例，`:629-773`）
- 流程总述：先在 AI 平台取密钥，回 LIRA 选平台/粘贴密钥/选模型/测试连接（634）。
- 6 步外链截图流程：登录 DeepSeek 开放平台（644，图 `deepseek-login.webp`）→ 完成账号验证（666，图 `deepseek-email-later.webp`）→ 左侧菜单`「API keys」`→`「创建 API key」`（682，图 `deepseek-api-keys.webp`）→ 命名（如 LIRA）后`「创建」`（699，图 `deepseek-create-key.webp`）→`「复制」`密钥，只显示一次（716，图 `deepseek-copy-key.webp`）。
- 回 LIRA：`「百宝箱 → 弹幕姬 → AI 互动助手」`→`「AI 平台」`选`「DeepSeek 官方」`，地址自动填好（733）。
- 粘贴`「密钥（API Key）」`等`「已保存」`，也可点`「保存设置」`；保存后不完整显示（739）。
- `「获取模型」`选模型 →`「测试 AI 连接」`→ 填自定义触发关键词等`「已保存」`→ 打开`「启用 AI」`→ 发含关键词弹幕验证（745）。
- 用量与充值：DeepSeek 用量页/价格页外链；充值后回 LIRA 再点`「测试 AI 连接」`（751-752，图 `deepseek-usage.webp`、`deepseek-recharge.webp`）。

### 10 直播姬 / OBS 投屏设置（`:776-837`）
- 总述：复制链接加入直播姬「浏览器」/OBS「浏览器源」；弹幕姬用在线链接，点歌队列与歌词需 LIRA 与直播软件同机且 LIRA 开启；网页歌单与直播画面用各自链接（781）。
- 点歌队列画面：`「点歌板」`六种风格（经典队列、透明身份榜单、奶油蓝甜点画框、甜粉麦克风舞台、紫金星月梦境、奶油金唱片铃兰），按风格调整字号/配色/滚动（786）。
- 歌单展示板：循环滚动歌库全部歌曲，滚动速度与主题配色在`「展示板」`独立设置（792）。
- 歌词画面：显示当前播放歌词，支持逐字高亮（798）。
- 盲盒盈亏榜：实时展示盈亏排名，参数在礼物页`「盲盒盈亏榜」`卡片设置（804）。
- 加班机画面：地址在`「百宝箱 → 加班机 → 直播画面」`复制，背景可透明或内置样式（810-811）。
- 礼物特效画面：固定地址在`「百宝箱 → 礼物特效」`复制；无内容时透明（817-819）。
- 弹幕与礼物边框：弹幕姬显示消息，礼物姬提供金额门槛边框；分别从`「百宝箱 → 弹幕姬」`和「礼物姬」复制（825-826）。
- 游戏与场景画面：小游戏、转转盘、开播动画、萌时钟各有链接，可在`「点歌 → 浏览器源」`集中复制（832-833）。

### 11 常用设置在哪里（`:840-888`）
- 点歌规则（直播间号、接收开关、暂停点歌、排队歌曲数、同一观众点歌间隔、允许重复、只允许歌库歌曲）在`「点歌 → 设置」`，会同步同账号；暂停点歌不停止接收礼物（849-851）。
- 主题美化（颜色主题、背景透明度、毛玻璃模糊、发光强度、渐变色）在`「点歌板」`；支持一键预设与`「一键毛玻璃美化」`（857）。
- 投屏文字（歌名/标题/点歌人字号独立调节、字体、字重、滚动模式（弹跳/连续）、滚动速度）在`「点歌板」`（863）。
- 礼物与盲盒：礼物统计/提示/冲刺目标在礼物页；官方盲盒关系自动获取，自定义成本与产出在`「盲盒设置」`；历史同步状态在礼物记录中；投屏在`「盲盒盈亏榜」`（869）。
- 签到与回复：签到机器人、抽签机器人、随机点歌回复、自定义关键词回复开关都在`百宝箱 → 弹幕姬`，规则与词库在同面板折叠区域；AI 触发词/模型/密钥也在弹幕姬（875-877）。
- 歌单展示板：滚动速率（1 最慢、100 最快）、排序方式、字号、是否同步点歌板主题在`「展示板」`（883-884）。

### 12 常见问题（14 条，`:891-1275`）
明细见 §5。

---

## 2. 有功能但文档完全没写

### 2.1 完全缺失的整块功能（最严重）

| # | 功能 | 入口 | 关键界面文案/默认值 | 证据 |
| --- | --- | --- | --- | --- |
| 1 | **动态抽奖**（管理抽奖专用账号、从动态/视频抓评论开奖） | 百宝箱 → 直播互动 → 动态抽奖 | `抽奖账号`、`请使用内容作者账号；独立登录，不影响直播账号。`、`登录账号`/`管理账号`/`刷新状态`/`退出账号`、`动态或视频链接`、`中奖人数`(1–100，默认 10)、`附加条件`(`关注作者`默认勾选/`点赞`/`转发`)、`评论参与 · 每人一次 · 作者不参与`、`获取参与名单`、`暂停获取`/`继续获取`/`开始抽奖`、`抽奖规则`(7 条)、`中奖结果`、`开奖凭证`、`本地记录 · 最近 50 场` | `public/pages/admin/toolbox/dynamic-lottery.html:11-143`；侧栏入口 `public/pages/admin/toolbox/shell-start.html:165-201` |
| 2 | **粉丝档案**（资料/互动/提醒，含备份恢复） | 百宝箱 → 主播工作 → 粉丝档案 | 页签 `档案`/`提醒`/`设置`、`新建档案`、筛选 `全部`/`在舰`/`曾上舰`/`特别关注`/`待补资料`/`待核实`、`同步大航海名单`、`自动更新`（每天 12:10 核对大航海身份，错过后当天首次打开补更新）、`导出档案表格`、`保存完整备份`、`从备份文件恢复`、`恢复到之前的状态（恢复点）`、`不再自动建档的粉丝`、`已归档的档案`、`清除全部档案` | `public/pages/admin/toolbox/fan-profiles.html:10-194`；导航 `fan-profiles-nav.html:1-37` |
| 3 | **投票与评分**（弹幕投票 / 观众评分） | 百宝箱 → 小游戏 → 类别 3 投票与评分 | `投票主题`(≤60 字)、`投票选项`(每项 ≤10 字)、`投票时长`（分 0–60、秒 0–59，默认 1 分 0 秒，预设 1/2/5 分钟）、`开始投票`；`评分主题`、`评分范围`固定 1–10 整数、`收集中不显示平均分…由你停止后统一公布`、`开始评分`、`结束本场`/`取消本场`；`浏览器源 · 推荐 800 × 600`、`透明背景`、`画面样式`（标题/提示文字/评分规则/文字色/进度色/轨道底色/不透明度 0–100 默认 100/文字大小 16–24 默认 20px/条形圆角 0–32 默认 20px/显示状态/显示人数）、`投票与评分每次仅运行一场，共用一个直播画面` | `public/pages/admin/toolbox/games.html:410-542`；样式模块 `public/js/admin/interaction-appearance.js` |
| 4 | **礼物许愿** | 礼物姬 → `礼物许愿` 页签 | 三个周期 `长效许愿`/`本日许愿`（北京时间零点换日）/`本场直播许愿`（下次开播重新计数）、`目标数量`(1–999999999，默认 10)、`许愿备注`(≤40 字，`选填，不在直播画面显示`)、`OBS 建议宽度 440；每份心愿约 96 高…背景透明`、礼物选择器（`本直播间`/`全部缓存礼物`） | `public/pages/admin/toolbox/gift-wishes.html:1-50`；页签 `public/pages/admin/toolbox/gift.html:13`；逻辑 `public/js/admin/gifts/wishes.js:15-19,175-179` |
| 5 | **点歌确认悬浮通知**（观众点歌时在管理页实时弹窗确认） | 全局浮层（点歌页/播放页） | 标题 `收到点歌请求`，按钮 `✓ 下一首播放`、`✗ 跳过` | `public/pages/admin/shared/song-confirmation.html:1-21`；渲染 `public/js/playback/core/renderer.js:147`；点击处理 `public/js/playback/core/event-handlers.js:125`；变更记录 `UPDATE.md:2205` |
| 6 | **礼物边框（礼物姬 页签 1，默认页签）** | 礼物姬 → `礼物边框` | `启用礼物边框`、`触发金额（元）`默认 **20**（`按最终礼物金额判断；0 元表示所有付费礼物。`）、`边框主题`（`林间花信`）、`动效模式`（`跟随系统`/`完整动效`/`简化动效`）、预览输入（观众/礼物/数量/金额，默认 观众A/林间花信/2/520）、`保存礼物边框设置`、`播放预览`、`OBS / 直播姬地址` | `public/pages/admin/toolbox/gift.html:11,15-134`；默认值 `src/bilibili/gift/frame-config.js:7` |
| 7 | **滚动礼物（礼物姬 页签 2）完整参数** | 礼物姬 → `滚动礼物` | 价格分色区间（浅蓝 0～/紫蓝/粉红/橙金，`包含起始金额、不含结束金额；相邻区间自动衔接`）、`显示行数`(1–10)、`滚动速率（1–50）`、`最小礼物金额（元）`（`0 表示不限制`）、`OBS 建议宽度 900，高度随显示行数调整`、`关闭此面板后仍继续播放`、`恢复默认`/`取消修改` | `public/pages/admin/toolbox/gift.html:12,135-198` |
| 8 | **礼物历史抽屉的筛选/排序/多选/导出** | 礼物 → 最近礼物 → `查看全部` | 筛选 `用户`/`礼物`/`金额大于（元）`/`开始日期`/`结束日期`/`今天`/`重置`/`查询`；列排序（时间/礼物/金额/备注）；`选择全部筛选结果`、`导出所选`、`点击整行选择 · 拖动框选本页 · Ctrl 拖动追加`、分页；导出页 `输出方式`（`拼成一张`/`每条一张`）、`图片背景`（`透明`/`白色`）、`保存文件夹`（`选择文件夹`/`系统默认`）、`导出 PNG`、`打开文件夹` | `public/pages/admin/gifts/history.html:56-168` |
| 9 | **弹幕姬屏蔽设置** | 百宝箱 → 弹幕姬 → `弹幕屏蔽设置` 区域 | `用户黑名单`（按 UID 屏蔽，昵称变化也有效）、`读取当前直播间观众`、`查找观众`（`搜索昵称或 UID`）、`加入黑名单`；`敏感词屏蔽`（`正文包含任一屏蔽词就不显示，英文不区分大小写`）、`一键清空屏蔽词`、`重新读取`、`添加、移除后自动保存…仅影响弹幕姬显示` | `public/pages/admin/toolbox/danmaku.html:115-169` |
| 10 | **弹幕姬样式与参数细节** | 百宝箱 → 弹幕姬 → `弹幕姬` 区域 | 6 种固定样式（`聊天气泡`/`深色面板`/`蝴蝶结`/`经典样式`/`透明文字`/`头像横卡`）+ 3 种全屏随机样式（`简洁白卡`/`奶油气泡`/`流光气泡`）；参数 `字体`（`推荐字体置顶，下方列出本机已安装字体`）、`正文字号` 18–48px（默认 30）、`弹幕文字颜色`、`背景不透明度` 0–100%（默认 100）、`礼物通知图片`（`使用样式插画`/`使用对应礼物原图`）、`停留（秒）` 2–30（默认 6）、`恢复当前样式默认值`、`读取服务器配置`、`打开网页` | `public/pages/admin/toolbox/danmaku.html:170-371` |
| 11 | **弹幕姬「进场欢迎」「PK 对手信息播报」** | 百宝箱 → 弹幕姬 → `固定回复` 列表 | `进场欢迎`（参数/词库两个视图、`刷新`）、`PK 对手信息播报`（`PK 开始时，自动在本直播间播报对手的大航海和在线榜概况`、`金额按当前贡献值估算（1 元＝10 贡献值），三个档位不重复统计`、`刷新状态`）、`昵称生僻字自动注音`（`读音会随欢迎、问候弹幕公开发送`）、云端状态 `刷新云端状态`/`连接云端账号后可设置` | `public/pages/admin/toolbox/danmaku-welcome-switch.html:1-10`；`danmaku-pk-report-switch.html:1-10`；`danmaku.html:495-502,578-581,574-577` |
| 12 | **自动答谢礼物 / 弹幕查询礼物统计（云端常驻）** | 礼物页顶部控制区（登录 B 站并同步后出现） | `自动答谢礼物`→`开启自动答谢`（`答谢付费和已确认免费礼物，不含醒目留言。`）；`弹幕查询礼物统计`→`开启弹幕查询`（`观众可用"今日礼物"等弹幕查询已记录的付费礼物。`）；`由服务器持续运行，退出软件后仍有效。登录 B 站并同步后可手动开启。`、`刷新云端状态` | `public/pages/admin/gifts/page.html:32-74`；逻辑与错误文案 `public/js/admin/gifts/interaction-controls.js:4-13,39-43` |
| 13 | **播放器音质选择 / 音量 / 播放队列弹窗入口 / 清理缓存** | 播放页底栏与顶部 | `选择播放音质`（默认显示 `标准`）、`音量`（默认 30%）、`播放队列`按钮、`清理缓存`、`退出登录` | `public/pages/admin/playback/page.html:36-37,39-46,528-558,559-643` |
| 14 | **点歌匹配诊断** | 播放页折叠面板 | `歌曲名`/`歌手`/`时长毫秒`/`测试匹配`、`用本地歌库测试歌曲匹配准确度`（文档仅在 FAQ 提到「没有结果」） | `public/pages/admin/playback/page.html:292-330` |
| 15 | **收藏/歌单浏览抽屉** | 播放页 `快捷入口` → 卡片 → 右侧抽屉 | `播放全部`、`随机播放`、`换一批`、`播放全部`（顺序播放时从第一首开始，随机播放时从随机一首开始） | `public/pages/admin/playback/drawer.html:1-84` |
| 16 | **播放队列弹窗的「导入点歌」** | 播放页 → `播放队列` | `导入点歌`、`清空` | `public/pages/admin/playback/queue-popup.html:7-10` |
| 17 | **歌库「点歌价格」与「歌切 / 核对平台 / 核对备注」** | 点歌 → 歌库 | `点歌价格`（≤1000 字，`留空时网页不显示价格`，快捷填写 `免费`/`舰长`/`提督`/`总督`/`SC（填写金额）`，`价格预览`）、`歌切`（`链接 / BV号 / 时间点 / 说明`）、`核对平台`、`核对备注`；列表按 `首字母`/`点歌价格`/`状态` 等列展示 | `public/pages/admin/song/library.html:37-88,186-198`；长度限制 `src/music/song-import-update.js:80` |
| 18 | **歌库多条件筛选与搜索** | 点歌 → 歌库 | `搜索`（歌名、歌手、分类或标签）、`分类`（可多选）、`语言`、`歌手`、`标签`（可多选）、`只看可点`（`全部歌曲`/`只看可点`）、`歌库共 N 首` | `public/pages/admin/song/library.html:98-182` |
| 19 | **导入模式（仅新增 / 更新匹配歌曲 + 预览）** | 点歌 → 导入导出 | `导入模式`：`仅新增，跳过已有歌曲` / `更新匹配歌曲（先预览）`；`用表格中的空白内容清除原有信息`；`歌名和歌手都相同，才会更新原歌曲…分类清空后归为「默认」`；`预览更新`→预览表（数据行/歌曲/操作/差异/说明）→`确认更新歌库`；`默认仅新增…不会更新价格`；`表格填写说明` 折叠区（文件与表头、多个值用「/」和逗号、点歌价格、歌切与平台、清空歌库说明） | `public/pages/admin/song/import-export.html:37-117` |
| 20 | **清空歌库 / 清空歌曲和使用记录** | 点歌 → 导入导出 | `清空歌库`（`清空歌曲和分类，直播间号、主题及其他设置会保留。`）、`清空歌曲和使用记录` | `public/pages/admin/song/import-export.html:10-19,113-115` |
| 21 | **展示板独立主题（关闭同步后）完整参数** | 点歌 → 展示板 | `字体大小`（10–80，默认 28px，帮助文案写「默认 40 px」）、排序 5 项（按首字母/按歌曲风格/按歌手/按语言/按歌曲名字长度）、`同步点歌板主题`、预设配色、主/辅/文字/背景色、字体 14 项、字重 4 档、`歌名颜色`、`面板标题`（默认「可点歌单」）、`歌名字号` 10–40、`标题字号` 10–28、`背景模糊` 0–30、`发光强度` 0–20、`启用渐变`、`渐变结束色`、`背景透明度` 0–1、`圆角大小` 0–24、`恢复默认` | `public/pages/admin/song/song-board.html:1-334` |
| 22 | **点歌板 2–6 风格专属参数** | 点歌 → 点歌板（切换风格 2–6） | `插画文字`（字体/字重/自定义文字颜色）、`队列滚动`（循环/往返 + 上下滚动速率 1–100）、风格 2 专属 `置顶公告`（3 条）、`规则展示`（规则 1–6 + 各自颜色、`规则小块字号` 8–18px） | `public/pages/admin/song/queue-theme.html:405-637` |
| 23 | **点歌板「前台低占用模式」与序号显示** | 点歌 → 点歌板（风格 1） | `前台低占用模式`（`关闭毛玻璃、发光和重阴影，保留滚动效果。`）、`显示歌曲序号`、`序号颜色`、`仅在歌曲超过此数量时显示`（始终显示/超过 2/3/4/5 首）、`背景透明度`（百分比）、`圆角大小` | `public/pages/admin/song/queue-theme.html:254-330` |
| 24 | **桌面歌词的完整设置项** | 点歌 → 桌面歌词设置 | 基础样式（主字体/字体大小/字重/文字颜色/对齐方式（左/居中/右/两端对齐）/字间距/行间距）、描边与阴影（文字描边/描边颜色/描边宽度/文字阴影/阴影颜色/阴影强度/阴影模糊/阴影水平偏移/阴影垂直偏移）；另有独立文件 `desktop-lyric-layout.html`、`desktop-lyric-behavior.html`、`desktop-lyric-rendering.html`、`desktop-lyric-preview.html`（文档仅一句「字体、颜色、字号、描边和透明度」） | `public/pages/admin/song/desktop-lyric-appearance.html:1-342`；同级文件 `public/pages/admin/song/desktop-lyric-*.html` |
| 25 | **加时规则编辑器细节（含「添加礼物」选择器）** | 百宝箱 → 加班机 | `礼物时间规则`、`＋ 添加礼物`、`添加礼物` 对话框（`输入礼物名称或 ID`、`搜索全部礼物`）、`在售目录：未刷新`、`待结算 N`、`初始时长`（`手动输入` 格式 时:分；`设置初始时间`）、`背景`（`透明`/`深夜票券`）、`适配`（`铺满`/`完整显示`/`拉伸`）、`保存画面`、`打开画面`、`复制地址`、`礼物处理：未运行`/`礼物统计：关闭`/`加班机：关闭` 状态胶囊 | `public/pages/admin/toolbox/overtime.html:10-208`；模型 `public/js/admin/overtime-rule-model.js`、`overtime-rule-effect-editor.js` |
| 26 | **礼物特效「弹幕触发」与礼物 ID 查询入口** | 百宝箱 → 礼物特效 | `查找礼物 ID`（外链 `https://gifts.lirahub.cn/`）、`弹幕触发` 开关、示例 `礼物特效 32132` / `礼物特效32132` / `🎁 32132` / `🎁32132`、`特效按顺序播放` | `public/pages/admin/toolbox/gift-effects.html:10-33,93-115` |
| 27 | **开播动画完整参数** | 百宝箱 → 开播动画 | `显示整套特效（动画 + 音乐）`（`同时控制预览与固定浏览器源`）、`主标题`(≤20，默认「唱一首，在一首，给你的歌」)、`副标题`(≤40，默认「开播准备中」)、`主播名`(≤32)、`底部文案`(≤48，默认「欢迎来到直播间」)、`画质`（普通·推荐/高·更多氛围粒子/低·轻量模式）、`轨道动效`（心形巡航/灯带循环/流光进度）、`漂浮音符`、`音乐律动`、`人物图片`（PNG/JPG/WebP ≤16MB）、`开场音乐`（mp3/flac/wav/aac/ogg/m4a/wma）、`音乐音量`（默认 35%）、`16:9 实时画面`、`固定浏览器源地址` | `public/pages/admin/toolbox/start-animation.html:10-241` |
| 28 | **萌时钟完整参数** | 百宝箱 → 萌时钟 | 6 种风格（`桃桃便签`/`星夜软糖`/`汽水小鸭`/`横向刻度`/`竖向刻度`/`白字数显`）、`显示日期`、`显示秒数`、`小时格式`（24/12）、`角标文案`（≤16 字，默认「今天也要闪闪发光」）、`推荐浏览器源：580 × 210`、`打开预览` | `public/pages/admin/toolbox/clock.html:11-156` |
| 29 | **转盘加权语义** | 百宝箱 → 小游戏 → 转盘 | `每一份就是一次抽取机会，内容占比决定抽中概率。`、`总份数 N`、`+ 添加一项`、`保存设置`、`开始转动`、`至少配置两个选项后开始`、`需单独添加浏览器源；地址不随互动游戏改变` | `public/pages/admin/toolbox/games.html:348-408` |
| 30 | **小游戏各模式细节** | 百宝箱 → 小游戏 | 数字炸弹 `观众模式`（`指定 1 位观众`/`不限观众数量`）+ `对战观众`；五子棋 `观众输入示例 B2`、主播执黑先行、`对战观众`；你画我猜 `回合数`(1–12，默认 5)、`每局时长`(15–300 秒，默认 90)、`本场词库`（每类 100 词条、可多选、开局后锁定、全选/清空）、`本局题词`（仅主播可见）、快捷键（B 画笔/E 橡皮擦/L 直线/R 矩形/O 圆形/I 取色/Ctrl+Z 撤销/`[`/`]` 调粗细）、`结束作画`/`公布答案`/`开始下一题`、`刷新在线观众`、`结束当前游戏` | `public/pages/admin/toolbox/games.html:62-309` |
| 31 | **主播工作台完整参数** | 百宝箱 → 主播工作台 | `新建日程`（名称 ≤80、日期、时间、`全天`、分类 直播/工作/个人、备注 ≤500、`删除日程`）、日历（上/下月、`今天`、`本月 N 项安排`）、`备忘录`（≤2000 字，分类 `随手记`/`观众约定`/`复盘`、`保存备忘`）、`待办`（≤80 字，`未完成 N`/`已完成 N`）、`已保存到本机` | `public/pages/admin/toolbox/planner.html:1-352` |
| 32 | **性能页采样窗口与硬件信息** | 百宝箱 → 性能 | `每次检测采样 5 秒`、`本机硬件`（CPU 型号/核心/温度、GPU 型号/规格/温度、内存总容量/内存条）、`采样窗口：`/`检测时间：`/`本次服务进程：` | `public/pages/admin/toolbox/performance.html:15-20,114-265`；采样窗口 `public/js/admin/metrics.js:43` |
| 33 | **桌面更新的「自动更新」开关与「数据目录」** | 百宝箱 → 桌面更新 | `自动更新`（默认 `已关闭`）、`当前版本`、下载进度/传输量/速度、`本地数据与支持`→`数据目录`（`打开目录`）、`日志目录`（`打开目录`） | `public/pages/admin/toolbox/desktop-update.html:10-97` |
| 34 | **百宝箱 → 设置的官网/账号入口** | 百宝箱 → 设置 | `账户与网站`、`账号信息`（`登录账户`/`当前设备`）、`LIRA 官网`（`访问 lirahub.cn`）、`账号登录`（`https://admin.lirahub.cn/`）、`忘记密码？请联系管理员重置密码。` | `public/pages/admin/toolbox/settings.html:1-71` |
| 35 | **首次启动引导弹窗（onboarding）** | 首次打开自动弹出 | 7 步：`欢迎使用 LIRA`→`登录直播账号，连接直播间`→`把歌单导入歌库`→`选择你需要的音乐平台`→`要启用 AI 互动吗？`→`完整说明都在使用文档`→`新手引导已看完`；含 `稍后设置`、`上一步`、`退出引导` | `public/pages/admin/toolbox/onboarding.html:39-187`；逻辑 `public/js/admin/onboarding.js` |
| 36 | **文档搜索框** | 使用文档标题右侧 | `搜索文档：点歌、OBS、登录…`、`清空搜索` | `public/pages/admin/toolbox/usage-guide-search.html:1-19`；`public/js/admin/usage-guide-search.js` |

### 2.2 顶层导航与文档结构缺口

| # | 缺口 | 证据 |
| --- | --- | --- |
| 37 | 百宝箱侧栏实际有 **4 个分组、15 个工具**：直播互动（弹幕姬、礼物姬、动态抽奖、小游戏）、直播画面（加班机、礼物特效、开播动画、萌时钟）、主播工作（粉丝档案、主播工作台）、软件与帮助（设置、性能、使用文档、桌面更新）。文档 §08 只逐个介绍了 9 个，未提 `动态抽奖`、`粉丝档案` | `public/pages/admin/toolbox/shell-start.html:55-731` |
| 38 | 文档完全没有 `投票与评分`（小游戏类别 3）与 `礼物许愿`（礼物姬页签 3）的分类说明 | `games.html:410`；`gift.html:13` |
| 39 | 文档 §10 与 §05 均称浏览器源「全部 11 个」，但 `点歌 → 浏览器源` 目录页实际为 3 组共 11 项，且另有 5 个不在该目录页的浏览器源：`礼物边框`、`滚动礼物`、`礼物许愿`、`投票与评分`、`动态抽奖`（后者是否有独立 overlay 需人工确认） | `public/pages/admin/song/overlay-addresses.html:20,78,150`；`gift.html:124,188`；`gift-wishes.html:38`；`games.html:416`；服务端 overlay 作用域 `src/server/access-policy.js:8-16` |

---

## 3. 文档写了但已过时 / 与代码不一致

| # | 文档写法（行号） | 代码实际 | 证据 |
| --- | --- | --- | --- |
| 1 | **「折合多少个水晶球（2333 元/个）」**（`:443-444`） | 服务端常量 `CRYSTAL_BALL_VALUE_RMB = 100`，页面显示 `≈ 水晶球 N 个` = `Math.ceil(remainingRmb / 100)`。文档数值错误约 23 倍 | `src/bilibili/gift/query-service.js:13,181-182`；`public/pages/admin/gifts/page.html:242-245` |
| 2 | 「点击`「删除背景」`后恢复默认水彩背景」（`:312`） | 按钮实际文案为 `恢复默认背景`；后台提示为 `已恢复默认水彩背景。`。页面无「删除背景」按钮 | `public/pages/admin/song/import-export.html:221-228`；`public/js/admin/song-background.js:145-159` |
| 3 | 「单张不超过 5 MB」（`:311-312`）字符数与代码一致，但未写**上传失败提示原文**与格式错误提示 | `图片超过 5MB，请压缩后再上传。`、`仅支持 PNG / JPG / WebP / GIF 图片。` | `public/js/admin/song-background.js:3,6-7,125-128` |
| 4 | 「`「点歌 → 导入导出 → 网页歌单同步」`」路径（`:301`） | 面板标题为 `网页歌单同步`，但其包含两个 section：`网页歌单同步` 与 `歌单背景`；文档 §05 另立小节称「歌单页背景」（`:307`），与界面标题 `歌单背景` 不一致 | `public/pages/admin/song/import-export.html:119-236` |
| 5 | 「`「百宝箱 → 桌面更新 → 日志目录」`获取日志」（`:1013,1207`） | 实际为 `日志目录` 行的 `打开目录` 按钮，无独立「日志目录」导航项；同时存在 `数据目录` 行未被文档提及 | `public/pages/admin/toolbox/desktop-update.html:83-96` |
| 6 | 「`「点歌 → 浏览器源」`集中复制」（`:832-833`） | 页面首行明确要求「先在对应功能页完成样式和内容设置，再复制这里的固定地址。」且弹幕姬项需要 `应用到直播画面` 后才能复制（复制按钮初始 `disabled`）；文档未写此前提 | `public/pages/admin/song/overlay-addresses.html:3-5`；`overlay-addresses.html:92` |
| 7 | 弹幕姬「选择喜欢的样式」（`:512`） | 实测为 **6 种固定位置样式 + 3 种全屏随机样式**，且各处帮助文案写「六种固定位置样式或两种全屏随机样式」（与页面 3 个随机样式不一致，需人工确认） | `public/pages/admin/toolbox/danmaku.html:188,199,210,219,230,239`（6 固定）、`265,274,283`（3 随机）；`public/pages/admin/song/overlay-addresses.html:86`（写「两种」） |
| 8 | 「`「点歌板」`选择六种风格：经典队列、透明身份榜单、奶油蓝甜点画框、甜粉麦克风舞台、紫金星月梦境、奶油金唱片铃兰」（`:786`） | 6 个风格的界面主标题是 `点歌板风格 1`～`点歌板风格 6`，副标题才是上述名称；文档未给出「风格 N」与名称的对应关系，小白无法对号入座 | `public/pages/admin/song/queue-theme.html:12-54` |
| 9 | 「滚动模式（弹跳/连续）」（`:863`） | 下拉实际选项为 `循环滚动（首尾相接）` / `往返滚动（到底后返回）` | `public/pages/admin/song/queue-theme.html:64-67,507-511` |
| 10 | 「`「一键毛玻璃美化」`按钮」（`:857`） | 按钮文字含 emoji：`✨ 一键毛玻璃美化` | `public/pages/admin/song/queue-theme.html:398-400` |
| 11 | 「滚动速率（1 最慢、100 最快）」（`:883`） | 与帮助文案一致；但 `字体大小` 帮助文案写「默认 40 px」而输入框 `value="28"`，界面自相矛盾（需人工确认哪个为准） | `public/pages/admin/song/song-board.html:6-7,34-56` |
| 12 | 「`「歌词时间调整」`…最多调整 3 秒」（`:354`） | 实际为 `-3000 ~ 3000` **毫秒**、步进 50ms，另有 `重置` 按钮；文档未给单位与步进 | `public/pages/admin/playback/page.html:207-233` |
| 13 | 「返回数量可在 1–30 条之间调整」（`:343-344`） | 与 `min=1 max=30` 一致，但默认值为 **9**，文档未写默认值 | `public/pages/admin/playback/page.html:274-283` |
| 14 | 「点赞、转发…`「为你推荐」「每日推荐」「我喜欢」…歌单入口」（`:336`） | 快捷入口实际有 **7 个**：为你推荐、每日推荐、心动 / 电台、我喜欢、我的歌单、收藏歌单、最近播放。文档漏写「我的歌单」「收藏歌单」「最近播放」 | `public/pages/admin/playback/page.html:65-124` |
| 15 | 「`「心动 / 电台」`会在快放完时自动添加 10 首」（`:336`） | 与 `RADIO_REFILL_BATCH_SIZE: 10` 一致；但文档未写「心动/电台」会切换队列类型为「电台队列」（队列标题变化） | `public/js/playback/config.js:20`；`public/js/playback/queue/manager.js:283` |
| 16 | 「`「点歌 → 设置」`…队列上限」（`:272`）、「排队歌曲数」（`:849`） | 界面标签为 `最多排队歌曲数`，范围 `1–300`，服务端默认 **50**；文档既未写范围也未写默认值 | `public/pages/admin/song/settings.html:102-105`；`src/storage/settings-defaults.js:59`；`src/server/settings-contract.js:89-96` |
| 17 | 「同一观众点歌间隔（秒）」（`:272,849`） | 界面标签一致，范围 `0–3600`，默认 **0**，未在文档中出现 | `public/pages/admin/song/settings.html:106-109`；`src/server/settings-contract.js:95-96` |
| 18 | 「开启`「接收弹幕和礼物」`」（多处） | 是 `<select>` 而非开关，选项为 `暂不启用` / `启用`；「暂停接收点歌」选项为 `接收点歌` / `暂停点歌` | `public/pages/admin/song/settings.html:88-101` |
| 19 | 「`「保存设置」`」（多处） | 与登录/房间两个 fieldset 同处一个表单，提交按钮为 `保存设置`，一致；但文档未说明先填房间号再保存会即时显示房主资料 | `public/pages/admin/song/settings.html:55-59,125` |
| 20 | 「`「清空全部队列」`只移除点歌队列中的歌曲」（`:234`） | 点歌面板另有独立按钮 `清空 SC 记录`（文档未提）；`清空全部队列` 的语义需以 `src` 行为确认，文档表述与按钮名可对应（需人工确认是否真不影响 SC） | `public/pages/admin/song/shell-start.html:19-24,59-64` |
| 21 | 「`「最近结算」`保留最近 20 组已完成或忽略的礼物记录」（`:572-573`） | 页面帮助文案为 `显示最近 20 组已完成或忽略的礼物。`，与文档一致 | `public/pages/admin/toolbox/overtime.html:174-175` |
| 22 | 「`「礼物 → 最近礼物 → 查看全部」`选择记录并进入导出页」（`:547`） | 与页面一致；但导出页的实际选项名称为 `输出方式`/`图片背景`/`保存文件夹`/`导出 PNG`，文档用「输出方式、背景和保存文件夹」表述，措辞与界面不完全一致 | `public/pages/admin/gifts/history.html:134-166` |
| 23 | 「`「直播投屏」`卡片里的地址固定不变…也可以点`「打开预览」`」（`:588-590`） | 实际按钮为 `预览`（`giftEffectOpenBtn`），文案为 `预览` 而非「打开预览」 | `public/pages/admin/toolbox/gift-effects.html:86-89` |
| 24 | 「输入礼物 ID 点`「播放特效」`即可测试效果」（`:587`） | 一致；但文档未写礼物 ID 的来源入口 `查找礼物 ID`（`https://gifts.lirahub.cn/`），也没写弹幕触发语法 | `public/pages/admin/toolbox/gift-effects.html:10-33,93-115` |
| 25 | 「`「百宝箱 → 设置」`查看当前 LIRA 账号和这台电脑的信息。忘记密码、添加或移除设备，请联系管理员。」（`:496`） | 页面卡片标题为 `账户与网站`，字段为 `登录账户`/`当前设备`；另有 `LIRA 官网` 与 `账号登录` 两个外链入口未被文档提及 | `public/pages/admin/toolbox/settings.html:12-58` |
| 26 | 「查看电脑和 LIRA 的处理器、显卡、内存占用情况…`「电脑」`显示所有程序的占用；`「LIRA」`只显示本软件的占用」（`:611-612`） | 与卡片副标题一致；但文档未写「每次检测采样 5 秒」与硬件信息区 | `public/pages/admin/toolbox/performance.html:15-20,114-258` |
| 27 | 「`「暂停」`只会停下倒计时，收到礼物仍可能改变时间。要停止礼物改时，请关闭加班机。」（`:579`） | 页面/模型未找到直接对应文案，行为需要以 overtime 规则模型核对（*需人工确认*） | 页面 `public/pages/admin/toolbox/overtime.html:27-36`；模型 `public/js/admin/overtime-rule-model.js` |
| 28 | 「性能检测」名称（`:609`） | 侧栏与卡片显示为 `性能`；文档标题写「性能检测」 | `public/pages/admin/toolbox/shell-start.html:622-625` |
| 29 | 「`「随机点歌」`…多个条件用 + 连接」（`:247`） | 代码同时接受 `随机点歌 X`、`随机 X`、`随机X` 三种写法，且会剥离开头的 `+ ＋ : ： - —`；空格分隔的复合条件先按整词匹配、未命中再拆词。文档只写 `随机点歌` + `+` | `src/bilibili/bilibili-message-handler.js:142-161,174-180`；`src/music/random-song-filter.js:45-87` |
| 30 | 「歌名不完整时也会尝试匹配歌库」（`:232`） | 需要匹配服务佐证；`song-matcher` 存在时长容差 5000ms 且随机点歌可按歌手/标签/分类/语言筛选（*需人工确认*「不完整」的具体行为） | `src/music/song-matcher.js:45`；`src/music/random-song-filter.js:56-112` |
| 31 | 「歌库会自动同步…最多同步 5000 首」（`:303`）、FAQ（`:964`） | 与服务端限制一致（`count <= 5000`、`歌库超过 5000 首限制。`），文档正确 | `src/storage/song-store.js:7-10`；`public/js/admin/cloud-song-sync.js:28` |
| 32 | 「`「本机已登录」`只说明扫码成功」（`:128`） | 状态胶囊初始文案为 `本机未登录`，`lira-help` 中已包含与文档几乎相同的说明（文档是从界面复述，正确） | `public/pages/admin/song/settings.html:9,13-14` |
| 33 | 「`「礼物提示」`…会在 LIRA 右上角显示」（`:422`） | 与样式一致（`.toast-stack { right: 24px; top: 70px }`）；文档正确 | `public/css/admin/toasts/system.css:7-17` |
| 34 | 「`「清空全部记录」`会永久删除这个账号在本机和云端的全部礼物记录」（`:436`） | 与按钮 `title="删除当前账号在本机和云端的全部礼物记录"` 及确认文案一致；补充：服务端失败时页面显示 `云端记录已清空，本机记录尚未更新。` 等中间态，文档未提 | `public/pages/admin/gifts/history.html:46-53`；`public/js/admin/gifts/history-view.js:8-57` |
| 35 | 「礼物记录…断网时可以查看已保存的记录」（`:434`） | 代码中的实际提示文案为 `正在更新，当前记录可能不完整` / `更新较慢，请稍后重试。` / `记录暂未更新，请稍后重试。` / `暂时无法加载礼物记录。`；文档只写了两种提示 | `public/js/admin/gifts/history-view.js:22-57` |
| 36 | 「`「礼物统计」`…`「礼物提示」`…它们与加班机分别设置」（`:415`） | 正确；页面另有 `自动答谢礼物`、`弹幕查询礼物统计` 两组开关与云端状态，文档未写 | `public/pages/admin/gifts/page.html:32-74` |
| 37 | 「`「盲盒盈亏榜」`…排行人数（-1 显示全部，0 仅显示汇总，1 至 10 显示对应人数）」（`:468-470`） | 与帮助文案一致；但默认值为 **3**，`仅显示盈利` 默认**已勾选**，`仅显示心动盲盒` 默认未勾选，`投屏标题` 留空时默认标题为 `今日盲盒盈亏`。文档均未写默认值 | `public/pages/admin/gifts/page.html:291-334` |
| 38 | 「`「盲盒设置」`…常见盲盒的成本和可能开出的礼物会自动获取」（`:478`） | 另有 `高级 ▾`（自定义盲盒配置 JSON 编辑 + `保存 JSON`）、`展开其余盲盒 ▾`、`盲盒 ID（可选）`、`产物 ID:名称:价格` 输入格式、`若盲盒列表未更新，请完全退出客户端后重新打开。` —— 文档未写 | `public/pages/admin/gifts/page.html:351-421` |
| 39 | 「`「AI 平台」`中选择`「DeepSeek 官方」`」（`:733`） | 平台下拉实际有 6 项：`自动识别（兼容旧设置）`、`DeepSeek 官方`、`OpenAI 官方`、`Claude 官方兼容`、`Gemini 官方兼容`、`自定义 / 第三方兼容`；另有 `API 请求地址`、`接口协议`（自动识别/Responses API/Chat Completions）未被文档提及 | `public/pages/admin/toolbox/danmaku-ai.html:60-121`；`public/js/admin/ai-assistant-config-view.js:121-130` |
| 40 | 「设置回复长度、联网搜索和深度思考。是否支持这些功能，以所选平台和模型为准」（`:533`） | 回复长度字段为 `回复长度偏好`，范围 `10–50`，默认 **50**，帮助文案 `优先一条；内容较多时两条，必要时三条。`；还新增 `思考强度` 下拉（服务默认/最低/低/中/高/很高/最高）未被文档提及 | `public/pages/admin/toolbox/danmaku-ai.html:174-184,232-249` |
| 41 | 「调整同时处理的提问数、同一观众提问间隔、每分钟提问上限」（`:538`） | 与字段一致，默认值 `3` / `0` / `20`，范围 `1–5` / `0–3600` / `1–120`；另有只读的 `发送间隔（毫秒）`：`不同回复随机 500–2000 毫秒；同一回复分段随机 200–600 毫秒`；`AI 的性格和说话方式` 长度 20–8000。文档未写任何数值 | `public/pages/admin/toolbox/danmaku-ai.html:340-378` |
| 42 | 「按需接入和风天气、高德地图」（`:535-536`） | 实际字段：和风天气 `专属 API Host（无需 https://）` + `密钥（API Key）`；高德 `Web 服务 API Host` + `Web 服务 Key`；并有独立测试按钮 `测试和风天气`、`测试高德地图`（文档只写了一个`「测试 AI 连接」`） | `public/pages/admin/toolbox/danmaku-ai.html:272-325,385-398` |
| 43 | 「触发关键词没有默认值，需要主播自定义并保存」（`:530`） | 与 `maxlength="12"` + 帮助文案一致，文档未写长度上限 12 字 | `public/pages/admin/toolbox/danmaku-ai.html:165-173` |
| 44 | 「`「随机点歌回复」`，在观众点歌条件无匹配时自动 @ 提醒」（`:518-520`） | 与 `点歌未匹配时，会自动回复点歌人。回复自动生成，不用设置词库。` 一致，文档正确 | `public/pages/admin/toolbox/danmaku.html:492-494` |
| 45 | 「`「签到机器人」`」「`「抽签机器人」`」（`:523,875`） | 与页面一致；但两者均为**云端运行**（`云端运行 · 收到"签到"后回复累计天数`、状态 `尚未确认云端状态`、底部 `连接云端账号后可设置` + `刷新云端状态`），文档未写云端依赖与状态确认 | `public/pages/admin/toolbox/danmaku.html:465-472,574-577` |
| 46 | 「`轮数、间隔和词条配色`」（`:546`） | 界面上是 `显示行数`、`滚动速率（1–50）`、价格分色区间；「间隔」在界面中不存在对应字段（*需人工确认*是否指滚动速率） | `public/pages/admin/toolbox/gift.html:176-192` |
| 47 | 「`「按连击组」`每组结算一次；`「按具体数量」`收到数量 ×N 就结算 N 次」（`:568`） | 未在 HTML 中定位到这两组选项文案，选项可能由 `overtime-rule-*` 模块动态渲染（*需人工确认*） | `public/pages/admin/toolbox/overtime.html:122-126`；`public/js/admin/overtime-rule-effect-editor.js` |
| 48 | 「`「点歌页的「切歌」`会把第一首标为完成」（`:233`） | 按钮存在（`nextBtn` / `切歌`），行为需服务端佐证（*需人工确认*「标为完成」的准确语义） | `public/pages/admin/song/shell-start.html:53-58` |
| 49 | 截图基线 | 9 张图全部是安装程序或 DeepSeek 后台，**没有任何一张 LIRA 客户端界面截图**；文档正文里出现的所有按钮名都没有视觉锚点 | `public/img/usage-guide/` 目录清单：`deepseek-api-keys`、`deepseek-copy-key`、`deepseek-create-key`、`deepseek-email-later`、`deepseek-login`、`deepseek-recharge`、`deepseek-usage`、`install-existing-location`、`install-existing-user`、`uninstall-lira` |

---

## 4. 写了但过于简略，不足以让电脑小白照着操作

判定标准：缺入口路径 / 缺操作顺序 / 缺失败后的下一步 / 缺默认值·单位·取值范围。

| # | 文档位置 | 现状 | 缺什么 | 补齐所需证据 |
| --- | --- | --- | --- | --- |
| 1 | `:216` 「复制对应功能的链接，添加到直播姬或 OBS」 | 一句话带过 | 缺「怎么在 OBS 里新增浏览器源」的 5 步操作序列（来源 → 浏览器 → 粘贴 URL → 宽高 → 确定）；缺直播姬对应叫法与入口 | `public/pages/admin/song/overlay-addresses.html:3-5` |
| 2 | `:329` 「连接有问题时，可点击`「检查音乐平台连接」`」 | 只给按钮名 | 缺按钮位置（播放页右上，状态胶囊 `QQ音乐待登录`/`等待检查` 同一行）、点击后看哪个字段、看到什么算正常 | `public/pages/admin/playback/page.html:33-52` |
| 3 | `:329` 「QQ 音乐和网易云音乐需要分别点击右上角的登录按钮」 | 未给按钮实际文字 | 按钮文字会随平台变化（`登录QQ音乐`），且登录后变为 `退出登录`；缺「切换平台后要重新登录」的说明 | `public/pages/admin/playback/page.html:38-46` |
| 4 | `:336` 「点击`「为你推荐」`…选择想听的歌，加入播放队列」 | 缺中间步骤 | 点卡片会打开右侧抽屉（`点击左侧卡片浏览内容`），需在抽屉里选歌，抽屉底部还有 `播放全部`/`随机播放`；文档完全没写抽屉这一层 | `public/pages/admin/playback/drawer.html:75-83` |
| 5 | `:343-344` 在线搜索 | 缺默认值与操作顺序 | `关键词` / `平台`（只读，`跟随当前平台`）/ `数量`（默认 9）三字段顺序未写；未写「必须先选平台再搜索」；未写结果区在哪 | `public/pages/admin/playback/page.html:264-289` |
| 6 | `:351-355` 全民 K 歌歌词同步 | 缺路径细节与失败下一步 | 未写 `全民 K 歌缓存文件夹` 输入框与文件夹图标按钮；未写状态三项（`客户端`/`缓存`/`歌词`）与 `未配置` 提示；未写失败时点 `重新检测` | `public/pages/admin/playback/page.html:129-253` |
| 7 | `:361-364` 播放器控制栏 | 缺可操作细节 | 未写 `音量`（默认 30%，垂直滑杆）、`播放队列`按钮、`添加到音乐歌单`的 `+` 圆形按钮、`选择播放音质`（默认 `标准`） | `public/pages/admin/playback/page.html:402-421,527-643` |
| 8 | `:386-391` 桌面歌词浏览器源 | 缺参数与失败下一步 | 未写样式设置项（对齐方式/描边/阴影等 9+ 项）与推荐源尺寸；未写地址是否需要重启 OBS 才生效 | `public/pages/admin/song/desktop-lyric-appearance.html:1-342` |
| 9 | `:415-416` 礼物统计开关 | 缺开关位置与状态文案 | 未写开关在礼物页左上第一张卡、文案为 `已开启`/`已关闭`、下方 `等待直播消息…` 状态行；未写 `礼物统计：关闭` 状态胶囊在加班机页也会出现 | `public/pages/admin/gifts/page.html:4-17`；`public/pages/admin/toolbox/overtime.html:24` |
| 10 | `:428` 「点击`「查看全部」`」 | 缺抽屉操作细节 | 未写抽屉标题为 `全部礼物记录`、`返回礼物（Esc）`、筛选区 5 个字段、底部翻页、`已选 0 条` 与 `导出所选` | `public/pages/admin/gifts/history.html:3-123` |
| 11 | `:436` 清空全部记录 | 缺确认交互 | 未写这是抽屉右上角红色按钮、需二次确认、清空后可能先出现 `云端记录已清空，本机记录尚未更新。` | `public/pages/admin/gifts/history.html:46-53`；`public/js/admin/gifts/history-view.js:11-16` |
| 12 | `:452-459` 今日盲盒盈亏 / 分析工作区 | 缺视图按钮文字与关闭方式 | 未写三视图按钮实际文字为 `按观众看`/`按盲盒看`/`开盒记录`、`清除选择`、右上角 `关闭` 按钮；未写分页文案 `第 N 页，共 M 页` | `public/pages/admin/gifts/blindbox-analysis.html:127-196` |
| 13 | `:478-479` 盲盒设置 | 缺填写格式 | 未写 `产物 ID:名称:价格` 的填写格式与示例 `35207:幸运泡泡:1.5`、`盲盒 ID（可选）`、`+ 添加`、`展开其余盲盒`、`高级 ▾` JSON 编辑 | `public/pages/admin/gifts/page.html:366-421` |
| 14 | `:496` 百宝箱 → 设置 | 缺页面名 | 界面标题为 `账户与网站`，文档称「百宝箱 → 设置」；未写 `登录账户`/`当前设备` 两个字段与 `访问 lirahub.cn` / `账号登录` 两个入口 | `public/pages/admin/toolbox/settings.html:12-58` |
| 15 | `:512` 弹幕姬样式应用流程 | 缺样式清单与参数含义 | 未写 6+3 种样式名、参数区（字号 18–48、背景不透明度 0–100%、礼物通知图片来源、全屏停留 2–30 秒）、`恢复当前样式默认值`、`读取服务器配置` | `public/pages/admin/toolbox/danmaku.html:170-371` |
| 16 | `:514-515` 发送弹幕 | 缺快捷键与副作用 | 未写 `Ctrl + Enter` 发送、`0 字` 计数、`自动钓鱼/打劫` 的真实行为（`自动发送 钓鱼×10、打劫×5，每 5 秒一条`）——这是有风险的操作却没有提示 | `public/pages/admin/toolbox/danmaku.html:398-419`；`public/js/admin/danmaku-tool.js:183` |
| 17 | `:530-538` AI 互动助手 | 缺默认值与取值范围 | 触发关键词 ≤12 字、回复长度 10–50（默认 50）、同时处理 1–5（默认 3）、同一观众间隔 0–3600 秒（默认 0）、每分钟上限 1–120（默认 20）、性格文本 20–8000 字、思考强度 7 档 | `public/pages/admin/toolbox/danmaku-ai.html:165-184,340-378` |
| 18 | `:546-547` 礼物姬 | 缺页签结构与默认值 | 未写三个页签（`礼物边框`/`滚动礼物`/`礼物许愿`）、边框 `触发金额（元）` 默认 20、`动效模式` 3 档、滚动礼物 `显示行数` 1–10 与 `滚动速率` 1–50、`最小礼物金额` 的「大于、不含等于」语义 | `public/pages/admin/toolbox/gift.html:10-14,41-70,176-192` |
| 19 | `:554-555` 小游戏 | 缺各游戏参数与共用链接的后果 | 未写数字炸弹 `观众模式`、五子棋坐标写法 `B2`、你画我猜 `回合数` 1–12 / `每局时长` 15–300 秒 / 词库多选 / 快捷键；未写「三款游戏共用一个直播画面，同时只能开一局」与 `结束当前游戏` | `public/pages/admin/toolbox/games.html:18-309` |
| 20 | `:562-579` 加班机 | 缺入口顺序与数值 | 未写 `手动输入` 的 `时:分` 格式、`设置初始时间`、`＋ 添加礼物` 与礼物搜索对话框、`在售目录：未刷新`、背景两项（`透明`/`深夜票券`）、`适配` 3 档、`保存画面` 后才生效 | `public/pages/admin/toolbox/overtime.html:40-169` |
| 21 | `:587-590` 礼物特效 | 缺礼物 ID 来源与触发方式 | 未写 `查找礼物 ID` 外链、`弹幕触发` 开关与 4 种弹幕写法；未写 `预览` 按钮 | `public/pages/admin/toolbox/gift-effects.html:10-33,93-115` |
| 22 | `:597-599` 开播动画与萌时钟 | 缺全部参数与推荐尺寸 | 未写开播动画 4 个文案字段与字数上限、`画质`/`轨道动效`、图片 ≤16MB 与音频格式、`音乐音量` 默认 35%；未写萌时钟 6 种风格名、`显示日期`/`显示秒数`/`小时格式`/`角标文案` 与 `推荐浏览器源：580 × 210` | `public/pages/admin/toolbox/start-animation.html:18-203`；`public/pages/admin/toolbox/clock.html:49-155` |
| 23 | `:605` 主播工作台 | 一句话 | 未写 `新建日程` 弹窗字段（名称/日期/时间/全天/分类/备注/删除）、备忘三分法与字数、待办两态筛选 | `public/pages/admin/toolbox/planner.html:22-350` |
| 24 | `:611-612` 性能检测 | 缺操作与读数 | 未写 `检测状态` 初始为 `未检测`、5 秒倒计时、六张卡片名称（`电脑处理器占用`/`电脑显卡占用`/`电脑内存占用`/`LIRA 处理器占用`/`LIRA 显卡占用`/`LIRA 内存占用`）、硬件区 | `public/pages/admin/toolbox/performance.html:9-112` |
| 25 | `:619-623` 桌面更新 | 缺状态与开关 | 未写 `当前版本` 胶囊、`自动更新` 开关（默认 `已关闭`）、进度条与 `已传输/速度`、`数据目录` 与 `日志目录` 两个 `打开目录` 按钮的区别 | `public/pages/admin/toolbox/desktop-update.html:10-97` |
| 26 | `:733-745` AI 助手配置 6–8 步 | 缺字段级校验提示 | 未写 `密钥保存后不会完整显示`、`获取模型` 可手填模型 ID、`测试 AI 连接` 失败后看哪里、`启用 AI` 的位置（卡片右上） | `public/pages/admin/toolbox/danmaku-ai.html:39-45,122-159,381-399` |
| 27 | `:75,105-109` 注册/登录 | 缺「注册并进入」之后 | 未写首次进入还有 **新手引导弹窗（7 步）** 与 **交互式引导气泡（8 步）**，以及引导可跳过、可从「使用文档 → 重新查看新手引导」重来 | `public/pages/admin/toolbox/onboarding.html:39-187`；`public/js/admin/interactive-tour-config.js:27-159`；`usage-guide.html:18-24` |
| 28 | `:106` 「密码要求见输入框旁的说明」 | 把关键信息外推 | 文档正文应直接给出密码规则（长度/字符要求）。注册表单片段不在 `public/pages` 内（登录界面由 Electron 主进程窗口承载），*需人工确认*具体规则 | `usage-guide.html:106`；*需人工确认* |
| 29 | `:443-444` 月底冲刺 | 缺交互位置与单位 | 未写目标在 `冲刺目标(元)` 输入框填写并点 `保存`、`重置本轮` 按钮、四个数值块 `目标`/`已收`/`还差`/`≈ 水晶球`；且水晶球换算值错误（见 §3-1） | `public/pages/admin/gifts/page.html:223-263` |
| 30 | `:284,832` 「浏览器源」集中复制 | 缺前提 | 未写必须先在各功能页 `保存`/`应用`，否则复制到的地址不会反映最新样式；弹幕姬还需 `应用到直播画面` 才可复制 | `public/pages/admin/song/overlay-addresses.html:3-5,92` |

---

## 5. 必须配截图但现在没有截图

现状：`public/img/usage-guide/` 仅有安装程序（2 张）、卸载程序文件名（1 张）、DeepSeek 后台（7 张）——**LIRA 客户端界面截图为 0 张**。

建议规格：统一 **WebP**，宽度 1200–1600 px（整窗截图用 1440×900 或 1512×945，16:10），局部放大图用 800–1000 px 宽、裁剪到目标控件 + 上下文各留 40–80 px；比例按内容取 16:10（整窗）或 4:3 / 1:1（局部）。截图内必须**先给关键元素加红框 + 序号标注**，再导出。

| # | 文档位置（行号） | 截什么 | 图中必须出现的关键元素 / 标注点 | 建议比例与尺寸 |
| --- | --- | --- | --- | --- |
| 1 | `:75`（快速上手·登录 LIRA） | 首次启动的登录/注册界面（`「注册新账号」`、`「登录已有账号」`、用户名/密码/激活码输入框、`「注册并进入」`） | ① 两个页签/按钮 ② 激活码输入框 ③ 提交按钮；若同屏还有 onboarding 弹窗，另出一张（见 #21） | 16:10，1440×900 |
| 2 | `:79,124-127`（连接直播间①②③） | `点歌 → 设置` 完整面板 | ① `🔐 直播账号登录` 区 ② `📱 扫码登录直播账号` 按钮 ③ `本机未登录` 状态胶囊 ④ `连接你的直播间` 区与 `直播间号或链接` 输入框（含 placeholder 示例） ⑤ `接收弹幕和礼物` 下拉 ⑥ `保存设置` 按钮 | 16:10，1440×900；另出局部放大图 1000×600 |
| 3 | `:126,1037`（`「刷新直播」`） | 顶栏右侧状态区 | ① `弹幕监听未启用` / 连接状态胶囊 ② `刷新直播` 按钮 ③ `退出程序` 按钮 | 4:1 裁切，1200×300 |
| 4 | `:214,231`（点歌队列与 SC 队列） | 点歌页顶部两个队列面板 | ① `SC 队列` + `清空 SC 记录` ② `点歌队列` + `随机点歌`/`切歌`/`清空全部队列` ③ `N 首` 计数胶囊 ④ 一条真实队列条目（含点歌人名） | 16:10，1440×900；队列条目局部 900×500 |
| 5 | `:233`（点歌确认悬浮通知） | `收到点歌请求` 浮窗 | ① 标题 `收到点歌请求` ② 歌名与匹配信息 ③ 点歌人 ④ `✓ 下一首播放` ⑤ `✗ 跳过` | 1:1，640×640 |
| 6 | `:268-294`（点歌页 7 个标签） | 点歌页管理面板标签行 | ① 全部 7 个标签文字（`歌库`/`设置`/`点歌板`/`展示板`/`浏览器源`/`导入导出`/`桌面歌词设置`）② 当前选中态 | 3:1 裁切，1400×450 |
| 7 | `:240`（歌库管理） | `歌库` 标签页 | ① 表单字段（含 `点歌价格` 与 `快捷填写` 下拉、`歌切`、`核对平台`、`核对备注`）② 筛选行 ③ `歌库共 N 首` ④ 表格列（含 `首字母`/`状态`/`点歌价格` 列） | 16:10，1440×900 |
| 8 | `:287-289,1269`（导入导出） | `导入导出` 标签页 | ① `下载导入模板` ② `选择歌单表格（支持 Excel、CSV、TSV）` ③ `或从 Excel 复制表格后粘贴` 文本域 ④ `导入模式` 下拉 ⑤ `导入歌库` ⑥ `表格填写说明` 折叠区 | 16:10，1440×900 |
| 9 | `:301-303`（网页歌单同步） | `网页歌单同步` 卡片 | ① `绑定账号` / `网页歌曲` 两个字段 ② `同步到网页` 按钮 ③ `查看网页歌单` 链接 ④ 替换警告文案 | 4:3，1000×750 |
| 10 | `:309-313`（歌单背景） | `歌单背景` 卡片 | ① 背景预览区 ② `上传背景` ③ `恢复默认背景` ④ `支持 PNG、JPG、WebP、GIF，最大 5 MB。` | 4:3，1000×750 |
| 11 | `:275-277,786`（点歌板风格选择） | `点歌板` 标签页顶部风格选择器 | ① 6 个风格卡片主标题 `点歌板风格 1~6` 与副标题名称 ② 当前选中高亮；用于建立「风格 N ↔ 名称」对应 | 16:10，1440×900 |
| 12 | `:857`（一键毛玻璃美化） | 点歌板底部按钮区 | ① `✨ 一键毛玻璃美化` ② `恢复默认` ③ 保存按钮 `保存点歌板主题` | 4:1 裁切，1200×300 |
| 13 | `:883-884`（展示板） | `展示板` 标签页 | ① `歌单滚动速率`（1–100）滑杆与 `慢/快` 端点 ② `歌曲排序方式` 5 个选项 ③ `字体大小` ④ `同步点歌板主题` 开关 | 16:10，1440×900 |
| 14 | `:329-330`（播放页平台与登录） | 播放页顶栏 | ① 三个平台页签（QQ音乐/网易云音乐/全民 K 歌）② `QQ音乐待登录` 状态胶囊 ③ `检查音乐平台连接` ④ `清理缓存` ⑤ `登录QQ音乐` | 16:10，1440×900；另出 1200×350 局部 |
| 15 | `:336`（快捷入口 + 抽屉） | 快捷入口 7 张卡片 + 右侧抽屉 | ① 7 个卡片名 ② 抽屉标题 `浏览内容` ③ 抽屉底部 `播放全部` / `随机播放` | 16:10，1440×900 |
| 16 | `:336,364`（底部播放器） | 播放器控制栏 | ① 封面/歌名/歌手 ② 进度条与时间 ③ `+` 加入音乐歌单按钮 ④ 播放模式按钮与模式文字（`顺序`）⑤ 上一首/播放/下一首 ⑥ `标准` 音质按钮 ⑦ 播放队列按钮 ⑧ 音量 30% ⑨ 折叠箭头 | 16:10，1440×900；另出 1600×360 局部 |
| 17 | `:351-355`（全民 K 歌歌词同步） | `播放 → 全民 K 歌` 面板 | ① `全民 K歌歌词` 标题 ② `重新检测` 按钮 ③ `全民 K 歌缓存文件夹` 输入框 + 文件夹图标按钮 ④ `保存并检测` ⑤ `歌词时间调整`（毫秒、`重置`）⑥ 状态三项 `客户端`/`缓存`/`歌词` ⑦ `等待检测` 胶囊 | 16:10，1440×900 |
| 18 | `:371-372`（播放队列） | 播放队列弹窗 | ① 标题 `播放队列` 与 `N 首` ② `导入点歌` ③ `清空` ④ 队列条目 | 4:3，1000×780 |
| 19 | `:415-422`（礼物统计与提示） | 礼物页顶部两张控制卡 | ① `礼物统计` 标题与开关（`已开启`）② `等待直播消息…` ③ `礼物提示` 与 `显示礼物提示` ④ 若已同步：`自动答谢礼物`、`弹幕查询礼物统计`、`刷新云端状态` | 16:10，1440×900；另出 900×500 局部 |
| 20 | `:422`（礼物提示弹窗） | 一次真实礼物 toast | ① 礼物名 `xN` ② 价格徽章 ③ `观众 送出` ④ 礼物图标 ⑤ 位于窗口右上 | 3:1 裁切，900×300 |
| 21 | `:75 → 首次进入`（新手引导弹窗） | `把直播间准备好` 引导弹窗（含步骤进度） | ① 标题 `LIRA 首次启动` ② 进度条 ③ 当前步骤标题 ④ `上一步`/`下一步`/`稍后设置`/`退出引导` | 16:9，1200×675 |
| 22 | `:18-24`（重新查看新手引导） | 使用文档顶部快捷操作区 | ① `快速上手` ② `常见问题` ③ `重新查看新手引导` ④ 搜索框 `搜索文档：点歌、OBS、登录…` | 3:1 裁切，1400×450 |
| 23 | `:428-436`（礼物历史抽屉） | `全部礼物记录` 抽屉 | ① `返回礼物` ② `清空全部记录`（红色）③ 5 个筛选字段 ④ 表格 7 列（含勾选列）⑤ `选择全部筛选结果`、`导出所选` ⑥ 底部翻页与 `第 N/M 页` | 16:10，1440×900 |
| 24 | `:547`（导出 PNG 子视图） | 导出面板 | ① `输出方式`（拼成一张/每条一张）② `图片背景`（透明/白色）③ `保存文件夹` + `选择文件夹`/`系统默认` ④ 预览与 `上一张`/`下一张` ⑤ `导出 PNG` | 16:10，1440×900 |
| 25 | `:443-444`（月底冲刺） | `月底冲刺` 卡片 | ① 四块数值 `目标`/`已收`/`还差`/`≈ 水晶球` ② `冲刺目标(元)` ③ `保存` ④ `重置本轮` | 4:3，1100×600 |
| 26 | `:452-459`（今日盲盒盈亏 + 分析工作区） | 礼物页盲盒卡片 + `今日盲盒分析` 全屏工作区 | ① 表头 `观众/盒数/盒型/总成本/开出价值/观众盈亏` ② `查看全部` ③ 工作区三个视图按钮 `按观众看`/`按盲盒看`/`开盒记录` ④ `看哪位观众`/`看哪种盲盒`/`清除选择` ⑤ 分页 | 16:10 ×2 张，1440×900 |
| 27 | `:468-471`（盲盒盈亏榜参数） | `盲盒盈亏榜` 卡片 | ① `投屏标题`（placeholder `默认：今日盲盒盈亏`）② `排行人数`（默认 3）③ `仅显示盈利`（默认勾选）④ `仅显示心动盲盒` ⑤ `捕捉链接` + `复制链接` | 4:3，1100×700 |
| 28 | `:478-479`（盲盒设置） | `盲盒设置` 卡片 | ① 输入行 `盲盒 ID（可选）`/`盲盒名`/`成本(元)`/`产物 ID:名称:价格` ② `+ 添加` ③ `高级 ▾` ④ `展开其余盲盒 ▾` ⑤ `若盲盒列表未更新…` 帮助 | 16:10，1440×900 |
| 29 | `:504-512`（弹幕姬样式） | 弹幕姬 `弹幕姬` 区域 | ① `直播画面链接` 输入框 ② `复制链接`/`打开网页` ③ `预览效果` ④ `应用到直播画面` ⑤ 6 种固定样式缩略图 ⑥ 3 种全屏随机样式缩略图 | 16:10，1440×900；样式网格局部 1400×700 |
| 30 | `:509-510`（弹幕姬连接状态） | 弹幕姬 `连接状态` 区 | ① `发送账号`/`直播间`/`发送状态` 三格 ② 刷新图标按钮 | 3:1 裁切，1200×400 |
| 31 | `:115-119`（弹幕屏蔽设置） | `弹幕屏蔽设置` 区 | ① `用户黑名单` + `用户 UID` + `读取当前直播间观众` ② `敏感词屏蔽` + `一键清空屏蔽词` ③ `重新读取` | 16:10，1440×900 |
| 32 | `:514-515`（发送弹幕） | `发送弹幕` 区 | ① 文本域（含 `输入内容，Ctrl + Enter 发送`）② `0 字` ③ `自动钓鱼/打劫` ④ `发送弹幕` | 3:1 裁切，1300×430 |
| 33 | `:523,875`（固定回复 / 签到抽签） | `固定回复` 概览列表 | ① `随机点歌回复` ② `进场欢迎` ③ `签到机器人`（`云端运行…`）④ `抽签机器人`（`云端运行…`）⑤ `自定义关键词回复` ⑥ `PK 对手信息播报` ⑦ `昵称生僻字自动注音` ⑧ 每个开关与 `说明`/`编辑`/`设置`/`详情` 按钮 | 16:10，1440×900 |
| 34 | `:733-745`（AI 配置回填） | `AI 互动助手` 卡片上半部 | ① `启用 AI` 开关 ② 状态三格 `配置状态`/`等待队列`/`运行模型` ③ `AI 平台` 下拉展开（6 个选项）④ `API 请求地址` ⑤ `密钥（API Key）` + `尚未保存` ⑥ `接口协议` ⑦ `获取模型` | 16:10，1440×900；下拉展开局部 900×600 |
| 35 | `:533,538`（回复规则 / 高级设置） | AI 回复规则 + 高级设置展开 | ① `触发关键词`、`回复长度偏好`（默认 50）② 能力栏 `协议`/`联网`/`深度思考` ③ `联网搜索`/`深度思考` 开关 ④ `思考强度` 下拉 ⑤ 高级设置数值字段与 `AI 的性格和说话方式` | 16:10 ×2 张，1440×900 |
| 36 | `:535-536`（扩展能力） | `扩展能力` 折叠区展开 | ① `和风天气` 的 Host + Key ② `高德地图` 的 Host + Key ③ `测试和风天气`、`测试高德地图` 按钮 | 16:10，1440×900 |
| 37 | `:810-811`（加班机画面） | 加班机 `直播画面` 区 | ① `背景`（透明/深夜票券）② `适配`（铺满/完整显示/拉伸）③ `保存画面` ④ `打开画面` ⑤ `复制地址` ⑥ 实时预览框 | 16:10，1440×900 |
| 38 | `:562-579`（加班机主控与规则） | 加班机面板 | ① 大倒计时与 `未启用` ② 三个状态胶囊 ③ `启用加班机`/`开始`/`暂停`/`重置` ④ `初始时长` 输入（`例如 02:30`）与 `设置初始时间` ⑤ `＋ 添加礼物`、`刷新在售礼物`、`在售目录：未刷新`、`待结算 N` ⑥ `最近结算` 列表 | 16:10 ×2 张，1440×900 |
| 39 | `:587-590`（礼物特效） | 礼物特效面板 | ① `查找礼物 ID` 外链 ② `礼物 ID` 输入（`例如 32132`）+ `播放特效` ③ `OBS 浏览器源地址` + `复制地址`/`预览` ④ `弹幕触发` 开关与 4 条示例 | 16:10，1440×900 |
| 40 | `:597-599`（开播动画） | 开播动画面板 | ① `显示整套特效（动画 + 音乐）` ② 4 个文案字段与字数计数 ③ `画质`/`轨道动效` ④ `漂浮音符`/`音乐律动` ⑤ `人物图片`/`开场音乐` 上传与 `音乐音量` ⑥ 右侧 `开播动画预览` 与 `固定浏览器源地址` + `复制地址` | 16:10，1440×900 |
| 41 | `:597-599`（萌时钟） | 萌时钟面板 | ① 6 个风格按钮名称 ② `显示日期`/`显示秒数` ③ `小时格式` ④ `角标文案`（`最多 16 个字`）⑤ `萌时钟网址` + `复制网址` ⑥ `推荐浏览器源：580 × 210` | 16:10，1440×900 |
| 42 | `:554-555`（小游戏） | 小游戏面板 | ① `类别 1 直播间互动` 与 `游戏网页` 链接/`复制链接`/`打开网页` ② 三张游戏卡与 `开始*` 按钮 ③ `刷新在线观众`/`结束当前游戏` ④ `类别 2 转盘` 与 `转盘网页` ⑤ 你画我猜展开后的 `回合数`/`每局时长`/词库/快捷键说明 | 16:10 ×2 张，1440×900 |
| 43 | 新增章节（投票与评分） | `类别 3 投票与评分` | ① `浏览器源` 按钮与 `推荐 800 × 600` ② `弹幕投票`/`观众评分` 页签 ③ `投票主题`/`投票选项`/`投票时长` 预设 ④ `画面样式` 与预览区标注 `透明背景`、`800 × 600` | 16:10 ×2 张，1440×900 |
| 44 | 新增章节（礼物许愿） | 礼物姬 → `礼物许愿` 页签 | ① 三个周期按钮 ② `心愿礼物` 选择器 ③ `目标数量`/`许愿备注` ④ 心愿卡预览 ⑤ `OBS 建议宽度 440…背景透明` | 16:10，1440×900 |
| 45 | 新增章节（粉丝档案） | 粉丝档案 `档案`/`设置` 两页 | ① `新建档案` ② 筛选胶囊（在舰/曾上舰/特别关注/待补资料/待核实）③ `同步名单` ④ `导出表格`/`保存备份`/`选择备份文件`/`查看恢复点` ⑤ 危险操作 `清除全部档案` | 16:10 ×2 张，1440×900 |
| 46 | 新增章节（动态抽奖） | 动态抽奖面板 | ① `抽奖账号` 状态与 `登录账号` ② `动态或视频链接`/`中奖人数`/三个附加条件 ③ `获取参与名单` ④ `名单获取进度` 表 ⑤ `抽奖规则` 折叠区 ⑥ `开奖凭证` | 16:10 ×2 张，1440×900 |
| 47 | `:605`（主播工作台） | 主播工作台 | ① `新建日程` 与日历 ② `备忘录` 与三个分类 ③ `待办` 两态筛选 ④ `已保存到本机` | 16:10，1440×900 |
| 48 | `:611-612`（性能） | 性能面板 | ① `检测状态` = `未检测` 与 5 秒圆环 ② `开始检测` ③ 六张占用卡片名 ④ `本机硬件` 三卡与温度 ⑤ 底部 `采样窗口`/`检测时间`/`本次服务进程` | 16:10，1440×900 |
| 49 | `:619-623`（桌面更新） | 桌面更新面板 | ① `当前版本` 胶囊 ② `检查更新`/`下载更新`/`重启并更新` 三按钮 ③ 进度条 ④ `自动更新` 开关（`已关闭`）⑤ `数据目录`/`日志目录` 与 `打开目录` | 16:10，1440×900 |
| 50 | `:781,1183`（OBS 浏览器源配置） | OBS「浏览器源」属性窗口（第三方软件截图，务必遮挡个人信息） | ① `URL` 输入框已粘贴 LIRA 地址 ② `宽度`/`高度` ③ `确定` 按钮；与 `<功能页推荐尺寸>` 并排对照 | 4:3，1000×750 |
| 51 | `:786,792,798`（投屏效果示例） | 三个 overlay 的实际直播画面效果 | ① `点歌板`（六风格各一张，或至少 2 张）② `歌曲展示滚动版` ③ `桌面歌词`（含逐字高亮与 `罗`/`译` 效果） | 16:9 或按源推荐尺寸，1280×720 |
| 52 | `:623,1013,1207`（日志/数据目录） | 资源管理器中的日志目录与数据目录 | ① 地址栏路径（`%APPDATA%` 之类需遮挡用户名）② 日志文件名 ③ `数据目录` 与 `日志目录` 的按钮位置对照 | 16:10，1200×750 |

---

## 6. 现有 FAQ 覆盖情况与遗漏场景

### 6.1 现有 14 条 FAQ 覆盖的故障场景

| # | 行号 | 标题 | 覆盖的故障场景 |
| --- | --- | --- | --- |
| 1 | 899 | 打开软件时无法登录 | 未注册/换机缺登录码；网络连接失败；账号停用/设备授权异常；`客户端协议不兼容`/`构建校验失败` |
| 2 | 923 | 换电脑后怎么登录 | 短效登录码领取与使用；同步范围；不要复制数据文件夹 |
| 3 | 947 | 换电脑后，歌库会同步吗 | 5000 首上限；多机并发修改互相覆盖 |
| 4 | 971 | 提示"当前设备已由另一个 LIRA 进程登录" | 重复启动客户端；`重试连接` |
| 5 | 994 | 页面空白或点了按钮没反应 | 未关闭弹窗/加载未完成；重启客户端；取日志 |
| 6 | 1018 | 弹幕发不出去 | 发送账号/目标直播间/发送状态异常；登录过期；连接异常 |
| 7 | 1042 | 签到机器人没有自动回复 | 开关未开；祝福语列表为空 |
| 8 | 1065 | AI 互动助手不回复 | `启用 AI` 未开；平台/密钥/模型错误；触发关键词未保存；频率限制 |
| 9 | 1090 | 随机点歌没有反应或找不到歌曲 | 歌曲不可点/条件不符；`随机点歌回复`；播放版本不对；诊断无结果 |
| 10 | 1115 | 礼物列表不更新 | 直播账号/房间号/接收开关；`礼物统计`；历史同步中；断网；登录过期；加载失败 |
| 11 | 1139 | 加班机时间不增加 | `启用加班机` 未开；规则未开；减少/清零/文字展板；连击未结算；`暂停` 语义 |
| 12 | 1164 | 直播姬 / OBS 投屏画面不显示或尺寸不对 | 链接复制错；本机画面要求同机+开启；在线链接需联网；刷新浏览器源 |
| 13 | 1212 | 如何卸载 LIRA，卸载程序在哪里 | 找安装目录；`Uninstall LIRA.exe`；Windows 设置卸载 |
| 14 | 1252 | 歌单表格导入失败 | 模板列名；另存为 .xlsx 或粘贴；CSV/TSV |

### 6.2 主播真实会遇到但 FAQ 未覆盖的场景

| # | 场景（界面文案/错误分支原文） | 触发条件 | 证据 |
| --- | --- | --- | --- |
| 1 | **`点歌队列已达到上限。`** | 队列歌曲数达到 `最多排队歌曲数`（默认 50，最大 300），观众继续点歌会被拒 | `src/music/queue-service.js:20-26`；`src/server/routes/queue-routes.js:15`；默认值 `src/storage/settings-defaults.js:59` |
| 2 | **`队列里已经有这首歌。`** | `允许重复点歌` 设为 `不允许` 时观众点重复歌 | `src/server/routes/queue-routes.js:15`；开关 `public/pages/admin/song/settings.html:117-123` |
| 3 | **`请先登录直播账号后再随机点歌。`** | 未登录直播账号时观众发随机点歌 | `src/server/bilibili-runtime.js:152` |
| 4 | **`当前已暂停接收点歌。`** | `暂停接收点歌` = `暂停点歌` 时观众点歌 | `src/bilibili/bilibili-message-handler.js:36`；`src/bilibili/diagnostics.js:66` |
| 5 | **同一观众两次点歌被冷却拦掉（无提示/仅日志）** | `同一观众点歌间隔（秒）` > 0（范围 0–3600）时观众连点 | `public/pages/admin/song/settings.html:106-109`；`src/storage/cooldown-store.js:1-2` |
| 6 | **`结果歌库超过 5000 首，无法完整同步；请先在歌库整理至 5000 首以内。`** | 导入/更新后本地歌库超过 5000 首 | `src/music/song-import-update.js:200-205`；`src/storage/song-store.js:7-10` |
| 7 | **`歌库超过 5000 首限制。`**（云端同步侧） | 云同步上传超过 5000 首 | `public/js/admin/cloud-song-sync.js:28` |
| 8 | **导入更新页 `无法读取更新内容，请使用完整模板或带表头的文件（最多 5000 行）。`** | 模板不完整或行数超限 | `public/js/admin/song-import-update.js:31` |
| 9 | **`点歌价格别名冲突：请保留一个价格文本` / `点歌价格超过 1000 长度限制`** | 表格同时出现 `点歌价格` 与 `点歌条件` 等别名列且内容不同；价格文本超 1000 字 | `src/music/song-import-schema.js:67`；`src/music/song-import-update.js:80` |
| 10 | **`图片超过 5MB，请压缩后再上传。` / `仅支持 PNG / JPG / WebP / GIF 图片。`** | 歌单背景上传超限或格式错误 | `public/js/admin/song-background.js:6-7,125-128` |
| 11 | **授权类错误：`授权已失效，请重新授权。` / `当前设备授权已被管理员撤销。` / `当前主播账号已停用。` / `无法连接授权服务器，请检查网络后重试。` / `连接授权服务器超时，请重试。`** | 上传背景时授权状态异常 | `public/js/admin/song-background.js:9-20` |
| 12 | **`请先登录 B 站，再同步到服务器。` / `B 站登录已失效，请重新登录并同步。` / `B 站登录验证超时，请稍后再试。` / `账号或房间已变化，请确认后重试。` / `设备授权已失效，请重新登录 LIRA。` / `尚未连接服务器，请恢复连接后重试。`** | 开关 `自动答谢礼物` / 弹幕查询礼物统计 时云端状态未确认 | `public/js/admin/gifts/interaction-controls.js:4-13` |
| 13 | **`关闭还没同步，服务器可能仍在运行。` / `已开启，服务器会持续运行。`** | 关闭自动答谢/弹幕查询但服务器未确认 | `public/js/admin/gifts/interaction-controls.js:80-88` |
| 14 | **`云端记录已清空，本机记录尚未更新。` / `礼物记录已清空，列表暂未更新。`** | 清空礼物记录后远端成功、本机失败 | `public/js/admin/gifts/history-view.js:11-33` |
| 15 | **`更新较慢，请稍后重试。` / `正在更新，当前记录可能不完整` / `记录暂未更新，请稍后重试。` / `暂时无法加载礼物记录。` / `暂无礼物记录` + `收到礼物后，记录会显示在这里。`** | 礼物流水同步慢、失败、空库 | `public/js/admin/gifts/history-view.js:41-67` |
| 16 | **筛选无结果 / 日期非法** | 礼物抽屉 `开始日期` 晚于 `结束日期`，或筛选条件无命中 | `public/pages/admin/gifts/history.html:56-68`；`src/bilibili/gift/history-filters.js:15` |
| 17 | **`礼物流水正在同步，进度可能尚未完整。` / `开播状态暂未确认，已暂停本场计数，稍后自动重试。` / `还未开播，心愿会在开播后开始计数。`** | 礼物许愿的三个周期状态异常 | `public/js/admin/gifts/wishes.js:112-123` |
| 18 | **`目标数量必须是 1–999999999 的整数。` / `请先选择一份心愿礼物。`** | 许愿表填写错误 | `public/js/admin/gifts/wishes.js:208-215` |
| 19 | **`这里还没有缓存礼物，可切换礼物范围重试。`** | 许愿礼物选择器无缓存礼物 | `public/js/admin/gifts/wish-picker.js:66` |
| 20 | **`连接云端账号后可设置` / `尚未确认云端状态` / `刷新云端状态`** | 签到机器人、抽签机器人需要云端账号 | `public/pages/admin/toolbox/danmaku.html:574-577,466-471` |
| 21 | **`请在已连接服务器的桌面客户端中设置` / `正在读取账号信息…`** | 进场欢迎在非桌面/未连服务器环境 | `public/js/admin/danmaku-welcome.js:305` |
| 22 | **`可发送，监听已连接` 之外的发送失败分支** | 弹幕姬发送弹幕被风控/未登录/房间不匹配 | `public/js/admin/danmaku-tool.js:250`；FAQ 6 只覆盖状态检查，未覆盖风控 |
| 23 | **`这是获取弹幕服务器信息失败，不是点歌逻辑失败。常见原因是直播平台风控、WBI 签名变化、缺少登录 Cookie 或网络/IP 被风控。`** | 弹幕连接建立失败 | `src/bilibili/danmaku/api-client.js:171` |
| 24 | **`弹幕监听未启用`**（顶栏常驻状态） | 未开启 `接收弹幕和礼物` 或连接未建立 | `public/pages/admin/shell-start.html:172`；`public/js/admin/state-renderer.js:74` |
| 25 | **`结果歌库`/点歌匹配失败（`点歌匹配诊断` 无结果）后的下一步缺失** | 观众点的歌匹配不到 | FAQ 9 只说「以歌库实际歌曲为准」，未给可执行动作（改歌名别名/加标签/加歌手） | `public/pages/admin/playback/page.html:292-330`；`usage-guide.html:1110` |
| 26 | **`等待刷新当前在线观众` / 无在线观众时无法开局** | 小游戏 `对战观众` 下拉为空，`刷新在线观众` 后仍无数据 | `public/pages/admin/toolbox/games.html:87-92,128-131,53-55` |
| 27 | **`至少配置两个选项后开始` / `总份数 0`** | 转盘未配置足够选项 | `public/pages/admin/toolbox/games.html:380,398-405` |
| 28 | **`当前没有进行中的游戏` / `结束当前游戏` 不可用** | 想中途结束游戏但状态未同步 | `public/pages/admin/toolbox/games.html:49-58` |
| 29 | **`B站限制了获取速度，请稍等；所选名单全部获取完成后才能开奖。` / `无法确认时停在当前候选人` / `候选人用尽时显示缺额`** | 动态抽奖抓评论限速、核验失败 | `public/pages/admin/toolbox/dynamic-lottery.html:99,116-117` |
| 30 | **`暂时不用 AI，可以点击「稍后设置」` 之后想再补 AI 设置** | 首次引导跳过 AI 后不知道入口 | `public/pages/admin/toolbox/onboarding.html:160-162` |
| 31 | **`歌曲已排队但播放版本不对`**（FAQ 9 已提，但未给「切歌/清空队列/换平台搜索」的动作链） | 平台匹配到翻唱/错误版本 | `usage-guide.html:1109`；`public/pages/admin/playback/queue-popup.html:7-10` |
| 32 | **`点歌板的样式改了但直播画面没变`** | 只点了 `预览效果`/只改了草稿未点 `应用到直播画面`/未点 `保存点歌板主题` | `public/pages/admin/toolbox/danmaku.html:114,356-370`；`public/pages/admin/song/queue-theme.html:639` |
| 33 | **`礼物边框`/`滚动礼物`/`礼物许愿`/`投票评分` 的浏览器源地址拿不到或复制按钮不可点** | 依赖登录/服务器状态，按钮初始 disabled | `public/pages/admin/song/overlay-addresses.html:92`；`public/pages/admin/toolbox/gift.html:124-133`；`gift-wishes.html:38`；`games.html:415-418` |
| 34 | **`刷新直播` 后仍连不上、需要重新登录直播账号的完整链路** | Cookie 失效/换房间/换账号 | FAQ 1、6 分散提到，未合成一条「直播间连接异常」排查树 | `usage-guide.html:918,1036-1037,1132-1134` |
| 35 | **`弹幕姬` 屏蔽设置误伤观众后如何恢复（一键清空屏蔽词/移除黑名单）** | 敏感词或黑名单配置后弹幕不显示 | `public/pages/admin/toolbox/danmaku.html:115-169` |
| 36 | **`自动钓鱼/打劫` 误开后如何停止** | 该按钮会持续每 5 秒发送 `钓鱼×10、打劫×5` | `public/pages/admin/toolbox/danmaku.html:409-415`；`public/js/admin/danmaku-tool.js:183` |
| 37 | **`已归档的档案`/`不再自动建档的粉丝` 误操作后如何找回** | 粉丝档案删除/归档后主播找不到人 | `public/pages/admin/toolbox/fan-profiles.html:163-185,59-62` |
| 38 | **`恢复点` 回滚导致资料退回旧状态** | 合并档案或恢复备份后需要回退 | `public/pages/admin/toolbox/fan-profiles.html:155-161` |
| 39 | **`动态抽奖` 账号与直播账号混淆** | 主播用直播账号去开奖 | `public/pages/admin/toolbox/dynamic-lottery.html:29` |
| 40 | **`LIRA 无法启动 / 桌面端连接失败（`等待桌面端连接`）`** | 桌面更新页在非 Electron 环境或 IPC 断开 | `public/pages/admin/toolbox/desktop-update.html:21` |
| 41 | **`自动更新` 开关打开后仍不自动升级** | 需要理解 `自动更新` 与手动 `检查更新` 的关系 | `public/pages/admin/toolbox/desktop-update.html:68-75` |
| 42 | **`更新包下载中断/校验失败` 的具体恢复动作** | FAQ 13「更新卡住了」只给「重新检查或下载」「从项目主页下载」，未覆盖校验失败/权限不足 | `usage-guide.html:1205-1207` |
| 43 | **`账号与设备` 中「已授权」胶囊异常 / `登录账户` 显示「正在读取…」不动** | 授权服务不可达 | `public/pages/admin/toolbox/settings.html:26-37` |
| 44 | **`性能` 页 GPU 温度/占用读不到（显示 `--`/`等待检测`）** | 显卡驱动或传感器不支持 | `public/pages/admin/toolbox/performance.html:58-111,196-207` |
| 45 | **`开播动画` 上传的图片/音乐播放不出来** | 格式或大小超限（图片 ≤16MB，音频 mp3/flac/wav/aac/ogg/m4a/wma） | `public/pages/admin/toolbox/start-animation.html:135,154,172` |
| 46 | **`音乐平台登录过期` 后播放中断** | QQ 音乐/网易云 Cookie 失效，页面会显示 `QQ音乐待登录` | `public/pages/admin/playback/page.html:35-46` |
| 47 | **`全民 K 歌` 未启动/缓存目录选错导致歌词不同步** | 状态三项 `客户端`/`缓存`/`歌词` 会显示异常，需点 `重新检测` | `public/pages/admin/playback/page.html:135-138,236-250` |
| 48 | **`点歌触发但没有弹出确认窗` 或 `误点跳过`** | `点歌确认悬浮通知` 未被任何 FAQ 提及 | `public/pages/admin/shared/song-confirmation.html:1-21` |
| 49 | **`盲盒列表未更新`（需完全退出客户端重开）** | 官方盲盒关系缓存过期 | `public/pages/admin/gifts/page.html:355` |
| 50 | **`成本未知的盲盒` 导致盈亏显示不准确** | 未配置自定义成本，盈亏按 0 成本估算会误导 | `usage-guide.html:459`（文档提了现象，FAQ 未给「去盲盒设置补成本」的动作） |
| 51 | **`投票/评分结果里没有某些观众`** | 只统计弹幕参与，且 `仅统计主评论/每人一次` 类规则 | `public/pages/admin/toolbox/games.html:453-455,541` |
| 52 | **`多个功能同时开导致卡顿`** | 需要指引到 `性能` 页看六项占用并关闭 `前台低占用模式` 之外的 overlay | `public/pages/admin/toolbox/performance.html:52-112`；`public/pages/admin/song/queue-theme.html:290-301` |

### 6.3 FAQ 结构性问题

1. **14 条 FAQ 中有 5 条（#2、#3、#13、#14 及部分 #1）本质是「操作说明」而非「故障排查」**，与正文 §02/§03 重复；建议把真正的故障场景（上表 #1–#8、#12–#14、#20、#24、#27、#29、#32、#49）补入，并给每条加「判断依据 → 一个动作 → 仍失败时的下一步」三段式。
2. **没有任何 FAQ 覆盖「观众侧」表现**（观众为什么点不了歌、为什么没被点到、为什么没收到回复），而 §6.2 的证据显示多数失败分支发生在观众命令被服务端拒绝时（`src/bilibili/bilibili-message-handler.js:32-36`、`src/music/queue-service.js:26`、`src/server/bilibili-runtime.js:152`）。
3. **没有任何 FAQ 覆盖新增的云端常驻能力**（自动答谢、弹幕查询礼物统计、签到/抽签机器人、PK 播报、进场欢迎）——这些都有「未连接云端 → 开关不可用」的分支（`public/js/admin/gifts/interaction-controls.js:4-13`、`public/pages/admin/toolbox/danmaku.html:574-577`），是主播最容易困惑的「开关点不动」问题。
4. **没有任何 FAQ 覆盖浏览器源/overlay 的「复制按钮不可点」**（依赖登录与 `应用到直播画面`/`保存` 前置状态）。

---

## 7. 优先修复建议（按投入产出排序）

1. **先修 3 处硬错误**：水晶球换算 2333 → 100（§3-1）；`删除背景` → `恢复默认背景`（§3-2）；`滚动模式（弹跳/连续）` → `循环滚动 / 往返滚动`（§3-9）。
2. **补 4 个完全缺失的整块功能章节**：投票与评分、礼物许愿、粉丝档案、动态抽奖（§2.1-1~4）——这是「有功能但文档 0 覆盖」的最大面积。
3. **补默认值与取值范围**：队列上限 1–300/默认 50、冷却 0–3600/默认 0、AI 五项数值、盲盒排行人数默认 3 与两个开关默认态、边框门槛默认 20、滚动礼物行数 1–10 与速率 1–50、搜索数量默认 9（§3-13/16/17/37/40/41；§4-17/18）。
4. **补「不可见的一层」**：收藏/歌单抽屉、播放队列弹窗、礼物历史抽屉、导出子视图、点歌确认浮窗、onboarding 弹窗（§2.1-5/8/15/16/35；§4-4/10）。
5. **按 §5 清单分批截图**：第一批 12 张覆盖高频路径（#2 设置面板、#4 队列、#6 标签行、#8 导入导出、#14 播放页顶栏、#16 播放器、#17 全民K歌、#19 礼物控制卡、#23 礼物历史、#29 弹幕姬、#34 AI 配置、#42 小游戏）。
6. **重构 FAQ**：按 §6.3 的三段式重写，优先补 §6.2 的 #1、#2、#3、#4、#12、#13、#14、#20、#24、#29、#32、#33、#49。

## 8. 需人工确认清单

| # | 事项 | 位置 |
| --- | --- | --- |
| 1 | `清空全部队列` 是否真不影响 SC 记录 | `public/pages/admin/song/shell-start.html:19-24,59-64` |
| 2 | 「切歌」把第一首「标为完成」的准确语义 | `public/pages/admin/song/shell-start.html:53-58` |
| 3 | 弹幕随机样式数量：页面 3 个 vs 帮助文案「两种」 | `public/pages/admin/toolbox/danmaku.html:265,274,283` vs `public/pages/admin/song/overlay-addresses.html:86` |
| 4 | 展示板 `字体大小` 默认值：帮助文案「40 px」vs 输入框 `value="28"` | `public/pages/admin/song/song-board.html:34-56` |
| 5 | 加班机「暂停」是否只停倒计时、仍受礼物改时 | `public/js/admin/overtime-rule-model.js`、`public/js/admin/overtime.js` |
| 6 | 「按连击组 / 按具体数量」选项是否由 JS 动态渲染及其准确文案 | `public/js/admin/overtime-rule-effect-editor.js` |
| 7 | 礼物姬「间隔」对应的界面字段 | `public/pages/admin/toolbox/gift.html:176-192` |
| 8 | 礼物历史抽屉的筛选/日期控件与 `docs/architecture/frontend/pages.md:230` 的「不显示名称搜索、日期范围控件」描述冲突（架构文档 vs 现行页面） | `public/pages/admin/gifts/history.html:56-68`；`docs/architecture/frontend/pages.md:230` |
| 9 | 动态抽奖是否有独立 overlay 浏览器源 | `src/server/access-policy.js:8-16`（未列出 lottery 作用域） |
| 10 | 注册密码规则的具体要求（文档让用户「看输入框旁说明」） | `usage-guide.html:106` |
| 11 | 「歌名不完整也会尝试匹配歌库」的具体匹配策略与容差 | `src/music/song-matcher.js:45`；`public/js/playback/services/match-service.js` |
| 12 | 首次启动时 onboarding 弹窗与交互式引导的先后顺序/是否同时出现 | `public/js/admin/onboarding.js`；`public/js/admin/interactive-tour.js:505-515` |
