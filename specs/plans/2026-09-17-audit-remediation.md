# 审计问题分阶段收尾（撤销后）

状态：产品修复按用户此前要求暂停；本次只移除不应修改的条目并核对回退。

## Goal

当前保留 54 条：27 条已处理、15 条明确修复候选、8 条待补证、4 条待规则/兼容裁决。另 31 条已从当前清单撤出，不再排期。

当前 66 个原编号仍有关联条目；另外 31 个原编号仅保留撤销记录。总计 97 个编号的历史处置完整可追溯。

## Non-goals

不启动新的产品修复，不扩大需求，不自动提交、发布或改变协议。历史取证材料独立保留。

## Current Behavior

前一轮真实性复核已完成；本次撤销 31 条。37 个产品拥有者文件的差异核对没有找到这些条目对应的多余产品修改，因此没有回退产品源码。共享文件中的必要修复和其它任务功能保留。

## Ownership

[当前逐项清单](<D:/Work/lira-audit/current-review-2026-09-18/current-review.md>) 是当前事实/范围入口；[执行进度](<D:/Work/lira-audit/复核与第四轮指导-2026-09-17/07-全量修复进度.md>) 跟踪当前保留项。

## Compatibility Constraints

保留正常 HTTP/WS/IPC、持久化、租户身份、日志脱敏、密钥保护及 Electron/OBS 能力。逐块处理相邻差异，禁止整文件或整仓回滚。

## Proposed Changes / Milestones

1. 撤销集合和历史备份：保存 31 条结论及每个 owner 的改动归属。
2. 当前文档与生成器：删除对应条目及实施建议，维持 54 个当前条目与 97 个历史编号处置一致。
3. 代码回退核对：有已实施的多余改动才恢复；本次结果为 0 个产品文件。

## 当前修复候选（15）

| 当前条目 / 原编号 | 当前状态 | 当前处理范围 |
|---|---|---|
| R4-HTML-TOKEN<br>CLI-SEC-1 | 仍有明确修复点（范围已收窄） | 优先设计权限收窄：桌面引导与 OBS/外部展示凭据分开，保留已授权互动能力。不能直接删注入或只隐藏按钮。 |
| R4-GAME-ROLE<br>BUG-028 | 仍有明确修复点（范围已收窄） | 修复游戏 owner 的参与身份判定，覆盖主播本人及主播自动回复；保留真正观众和已有选人规则。 |
| R4-REQ-BODY-DESTROY<br>CLI-RT-11 | 仍有明确修复点（范围已收窄） | 修复错误响应与连接回收顺序，保留大小预算，并覆盖滴流/继续发送数据。优先级低于数据安全。 |
| R4-SONG-IMPORT-LIMIT<br>BUG-034 | 仍有明确修复点（范围已收窄） | 应修复旧入口的提交前完整结果校验，和现代导入保持一致；不得自动删除超量歌曲。 |
| R4-4XX-AS-500<br>BUG-040 | 仍有明确修复点（范围已收窄） | 对调用者无效参数返回明确 4xx；对非法上游响应使用相应上游错误类别。保留 URL 白名单和安全日志。 |
| R4-AVATAR-COOKIE-FORWARD<br>CLI-SEC-15 | 仍有明确修复点（范围已收窄） | 建议移除头像取图请求中的 Cookie，保留 HTTPS/域名和图片响应检查；避免把不需要的秘密送出。 |
| R4-DEAD-BRANCHES<br>CLI-RT-7、CLI-RT-8、CLI-RT-9 | 仍有明确修复点（范围已收窄） | 修 songs 清空全部通知与 blindbox 设置应用；games 先补连接成功时的状态对账/失败恢复，不应强制所有全局快照塞入游戏状态。 |
| R4-DOM-REBUILD-MISC<br>BUG-024、CLI-RT-15、CLI-RT-16 | 仍有明确修复点（范围已收窄） | 按 owner 增加结果时序保护，区分内容变化与重绘；分别验证 queue、songs、blindbox，不要整组做大重构。 |
| R4-GIFT-TOAST-SOURCE<br>CLI-RT-19 | 仍有明确修复点（范围已收窄） | 改正缺值回退语义，补两种消息结构；此项是解析错误，不是已证明重复扣款/发奖。 |
| R4-LICENSE-CLIENT-ROBUST<br>CLI-SEC-18 | 仍有明确修复点（范围已收窄） | 优先修复激活失败不能保留旧密钥的数据安全问题；按接口契约补流式响应限额。构建摘要不要冒充远程可信证明，也不要给完整礼物目录重新加错误的旧行数上限。 |
| R4-LYRIC-ENDPOINT-METHOD<br>BUG-003 | 仍有明确修复点（范围已收窄） | 低优先级纠正首次请求：使用正确状态入口，或在保证首次快照恢复后去掉无效请求。 |
| R4-MUSIC-PLATFORM-LOOKUP<br>CLI-SEC-13 | 仍有明确修复点（范围已收窄） | 在 IPC/登录 owner 做显式平台枚举和 own-property 查找，创建窗口前拒绝非法平台。 |
| R4-PLAYBACK-SNAPSHOT-RACE<br>CLI-RT-21 | 仍有明确修复点（范围已收窄） | 修复 owner 的版本/顺序边界，覆盖旧请求晚于新卸载快照到达；保留重启后的客户端身份/代次语义。 |
| R4-WESING-CONFIG-SNAPSHOT<br>BUG-041 | 仍有明确修复点（范围已收窄） | 低优先级统一拥有者的配置变更传播，覆盖通用入口与专用入口；不要无差别改成每次采样重读全部设置。 |
| R4-WHEEL-SNAPSHOT<br>CLI-RT-17 | 仍有明确修复点（范围已收窄） | 补连接成功时的受控对账/重试即可解决该缺口；是否扩充全局 snapshot 应按协议设计决定。 |

## 待补证（8）

| 当前条目 / 原编号 | 当前状态 | 待验证条件 / 边界 |
|---|---|---|
| R4-BUG-019-GIFT-IDENTITY<br>BUG-019 | 条件风险待补证，暂不直接开修 | 未取得支持“异组同身份复用”的平台样本；不能把查询命中已完成行直接等同于丢单。 |
| R4-DEDUPE-EVICTION<br>CLI-RT-12 | 条件风险待补证，暂不直接开修 | 模块探针证明淘汰后可再次接收；未证明真实平台历史窗口能满足全部条件。 |
| R4-MUSIC-CACHE-STAT<br>BUG-033 | 条件风险待补证，暂不直接开修 | 本轮未重做目录规模/耗时实验，不能宣布已证实性能事故。 |
| R4-OVERTIME-NUM-BOUND<br>BUG-029 | 条件风险待补证，暂不直接开修 | 静态确认风险点，未证明真实业务消息达到有害工作量；无金融/礼物数据修改。 |
| R4-IDENTITY-GROWTH<br>CLI-RT-14 | 条件风险待补证，暂不直接开修 | 未复现内存耗尽；缓存上限不能以静默丢失必要身份资料为代价。 |
| R4-QQ-STREAM-MAP<br>BUG-035 | 条件风险待补证，暂不直接开修 | 本轮未压测；不能用数学上的累计 O(n²) 代替正常播放下的性能证据。 |
| R4-SERV-INDEX-ON-REQUEST<br>SERV-RT-13 | 条件风险待补证，暂不直接开修 | 本轮未构造大旧库测阻塞，不能确认当前用户会遇到超时。 |
| R4-WS-FRAME-OVERSIZE<br>CLI-RT-6 | 条件风险待补证，暂不直接开修 | 构造任意超大字符串只能证明保护触发，不能证明正常功能被破坏。CLI-RT-6 与订阅过滤不是同一机制。 |

## 待规则 / 兼容裁决（4）

| 当前条目 / 原编号 | 当前状态 | 当前处理范围 |
|---|---|---|
| R4-ADMIN-SESSION-REVOKE<br>SERV-AUTH-2 | 规则或兼容策略待裁决 | 已按用户要求暂缓。应先确定退出当前会话、退出全部会话、管理员主动吊销的语义，再决定会话标识或代次。 |
| R4-SERV-SYNC-EPOCH<br>SERV-RT-5 | 规则或兼容策略待裁决 | 问题真实，但修复需先选旧版兼容策略：拒绝/升级旧客户端，或让旧游标可跨清空判代。不能默默重置客户端或把旧号当新号。 |
| R4-ADMIN-HOST-GUARD<br>SERV-AUTH-1 | 规则或兼容策略待裁决 | 遵守用户暂缓要求；先解决管理浏览器唯一 Host 与工具 API 例外的边界，再实施路由限制。 |
| R4-AI-QUOTA-REFUND<br>BUG-030 | 规则或兼容策略待裁决 | 明确安全用量是按尝试、已接受还是成功计数；据此区分本地取消、传输不确定及业务失败。不要仅凭可能收费宣称已经超支。 |

## Verification

- node "D:/Work/lira-audit/current-review-2026-09-18/verify-current-docs.cjs"：检查删除范围、当前页面和原编号处置。
- 生成器 node --check、正常生成入口重跑与输出稳定性。
- 两仓 git diff --check、git status 与撤销前哈希比较；本次仅改变审计计划文档。
- 前一轮 275 项产品测试与 21 个探针为历史验证，本次未重跑，不计作此次新结果。

## Rollback Or Failure Handling

只按 [撤销与代码回退核对登记](<D:/Work/lira-audit/current-review-2026-09-18/withdrawal-manifest.json>) 的具体归属处理；[撤销前历史材料](<D:/Work/lira-audit/current-review-2026-09-18/archive/before-withdrawal-2026-09-18/README.md>) 保存撤销前文档。禁止用 reset/checkout 覆盖其它任务改动。

## Done When

撤销项不再出现在当前工作清单；生成器重跑不恢复它们；原编号处置无遗漏；产品文件保持必要修复及其它任务功能；文档范围与最终检查一致。
