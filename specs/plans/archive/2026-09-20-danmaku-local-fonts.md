# 弹幕姬本机字体与滑块修正

## Goal / Current behavior
字体下拉接入已有本机已安装字体清单，项目已有字体中推荐七种置顶，默认显示具体字体。现有选项只有四个别名；不透明度未使用公共参数滑块。

## Ownership / Compatibility
管理页复用 local-font-library 与 parameter-range。客户端 src/shared、public/js/shared 和服务器 src/lib、public/overlay 的字体合同保持一致。保留 default/sans/serif/kai 旧值；新值只接受一个带双引号、可转义的字体族名（与本机字体模块输出一致），不接受任意 CSS 或 URL。无权限、会话、存储结构或架构变更。不修改无关控件及运行数据，不发布。

## Steps / Verification
- [x] 扩展双方字体合同与服务器规范、验收/OpenAPI，测试旧值、新字体、非法值以及渲染。
- [x] 管理页接入公共字体库，七种推荐置顶，旧值映射具体名称；滑块复用公共组件并在回读时刷新。
- [x] 运行字体库、弹幕设置和双方合同/协议测试，检查两仓库 diff 与状态。

## Done when
本机字体能选取、预览、保存及回读；旧设置兼容；滑块轨道与滑块端点一致。聚焦测试通过并记录未做的运行时验证。回滚只撤销本任务变更。

## Verification — complete
客户端 33 项聚焦测试通过，覆盖字体库、设置草稿、IPC、权限及管理页。服务器字体合同、设置服务/路由、协议与文档测试通过；既有 OBS Playwright 测试通过。最终控制字符边界修改后相关客户端 15 项、服务器 23 项测试通过。

隔离 Electron 43 控件检查使用真实枚举与项目字体权限处理器（合成同意响应），得到七种推荐和 213 个额外本机字体；下拉成功选择 Academico。实际点击滑块两端得到 0/100，500px 控件的轨道与满值填充均为 482px，端点内缩 9px。检查不使用用户账号或运行中的应用；完整生产登录/保存未执行，以合成设置测试及 OBS 渲染测试验证。临时 Electron 已退出，临时目录删除遭工具策略拒绝，保留在系统 Temp。UI 检测仅报告未修改的既有侧边框样式。
