# v5.0.6 Release Validation Implementation Plan

## Goal / Current behavior
按照 RELEASE_GUIDE.md 完成 5.0.6 发布准备并执行客户端发布。现有工作区包含日期筛选、盲盒主题、直播地址和粉丝档案改动；display.js 的空 catch 未通过架构门禁。

## Ownership / Proposed changes
版本由 package.json、package-lock.json 和 UPDATE.md 管理；网页歌单地址由 public/js/admin/display.js 管理，test/frontend-song-board.test.js 验证。发布由 scripts/publish-release.js 执行。对现有功能仅修复经验证的发布阻塞，不扩大业务范围。

## Constraints / Non-goals
保留已有用户改动；不部署服务端，不修改测试默认 6 并发，不弱化断言或增加跳过。测试使用隔离数据与现有临时服务端检出；不操作用户运行中的客户端。任务前文件摘要和关键文件备份位于 C:\Users\Tom\AppData\Local\Temp\lira-release-5.0.6-5739cb83。

## Milestones / Verification
- [x] 更新版本和更新说明，修复地址读取失败路径，定向测试通过。
- [x] 启用 NSIS 前置条件，npm run verify 和 npm run verify:modularity 通过；完整测试 fail/cancelled/skipped/todo 全为 0。
- [x] 核对文件摘要，检查 git diff --check、最终 diff 和 git status --short，无运行数据或秘密材料；归档准备记录。

## Release sequence
准备完成后提交 v5.0.6，检查提交尾注，推送 main，执行 npm run release:win。核对远端 main 和标签提交一致、Release 为正式发布，exe/blockmap/latest.yml 的字节数与摘要匹配，更新清单和 app.asar 版本为 5.0.6。

## Failure handling / Done when
门禁不通过即停止后续发布，记录并解决原因；仅修正本任务增量，不重置现有改动，不移动已发布标签。准备完成条件为完整测试零失败零跳过、静态检查通过和 diff 审核。归档后按发布流程执行，整个任务完成需远端产物完整且工作区干净。

## Results
- 网页歌单 URL 解析失败显式回退到禁用状态；初次资料读取失败保留禁用状态，迟到失败不能覆盖已收到的新账号状态。定向测试 20/20 通过。
- npm run verify 的契约、文档 5/5、语法及架构 22/22 门禁通过；首次全量运行因命令传输中的反斜线转义破坏 NSIS 路径而失败。改用存在性已确认的正斜线路径后重跑，不修改或跳过安装器测试。
- 最终 npm test -- --test-reporter=tap：2949/2949 通过，fail/cancelled/skipped/todo 均为 0；测试报告耗时 71.642 秒，命令墙钟 72.190 秒，保持默认 6 并发。安装器测试全部实际运行。
- npm run verify:modularity：1294 文件、13 个已评审尺寸记录、0 errors。git diff --check 按仓库既有换行配置通过。
- 验证前后所有文件 SHA-256 相同。本任务仅在既有改动上更新版本/发布说明和 display.js 失败路径及对应测试；日志、任务前备份和验证输入摘要位于 C:\Users\Tom\AppData\Local\Temp\lira-release-5.0.6-5739cb83。
- 发布准备完成；后续提交、推送、打包和远端核验按 Release sequence 执行，结果以发布脚本日志和 GitHub Release 为准。
