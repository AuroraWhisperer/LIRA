# v4.2.9 Release Validation Implementation Plan

**Goal:** 按 RELEASE_GUIDE.md 验证、提交并发布当前工作区，生成 v4.2.9 Windows 安装包。

**Architecture:** 沿用 package.json、UPDATE.md 和 scripts/publish-release.js 的现有发布流程，纳入当前功能改动，仅修复发布阻塞。

**Tech Stack:** Node.js 24、Electron 43、npm、Git、GitHub CLI、electron-builder。

## Current behavior / Ownership / Constraints

main 与远端当前均为 93dc215（v4.2.8），v4.2.9 尚不存在。版本及依赖锁由 package.json / package-lock.json 拥有，UPDATE.md 提供 Release 正文，现有发布脚本负责标签、上传与附件摘要。保留当前礼物、粉丝档案、管理页及文档改动；不部署或提交配套服务器，不操作真实用户数据，不改变构建设施及权限边界。

## Milestones / Verification

- [x] 对比 v4.2.8，更新 package.json、package-lock.json 与 UPDATE.md。
- [x] 使用现有锁定服务器检出执行 npm run verify，失败时仅修复发布阻塞并重跑受影响检查。
- [x] 检查最终 diff、git diff --check 和 git status --short，确认无运行数据、凭据或生成产物；记录结果并归档准备计划。

## Release / Failure handling / Done when

验证通过后按 guide 提交 v4.2.9，检查无 Claude Co-Authored-By，推送 main 并运行 npm run release:win。核对远端 tag 与发布提交一致，三个附件已上传且摘要匹配，latest.yml 的版本和 SHA-512 正确。失败保留现场，仅修复阻塞；不移动已发布 tag，不覆盖已有改动。准备阶段完成后归档，发布结果单独报告。

## Results

- 首轮契约与文档通过，918 个 JavaScript 语法检查通过；发现使用指南 CSS 806 行超过既有 798 行上限。仅将窄屏目录的两个规则移至已有 usage-guide-navigation.css，所有选择器和属性保持原值，正文文件降至 796 行。
- 全套首轮出现旧 PNG 宽度断言（1008，当前为 856）、AI 触发词旧 placeholder 断言，以及一次 Windows 原生进程归属查询失败。同步两个旧断言，保留实际 Electron 隔离、凭据、图像和截图检查；Windows 查询不改代码。直接相关检查 42/42 通过。
- 最终 npm run verify 全部通过：5 个锁定服务器夹具、文档 5/5、918 个 JavaScript 语法、架构 22/22；npm test 共 2525 项，2521 通过、0 失败、4 项既有 NSIS 集成测试因未配置测试环境跳过。Windows 原生查询在最终完整测试中通过。
- 检查期间新增更新页面移除项目主页和主播登录入口的两处改动，已检查并纳入；相关界面和路由测试另跑 33/33 通过。最终源码文件指纹核对一致，此后仅补充发布说明和本验证记录。
- 完整验证日志位于系统临时目录 lira-v4.2.9-verify-final.log。未引入用户数据、凭据或生成产物；准备阶段完成，提交、推送和发布结果单独报告。
