# v4.2.8 Release Validation Implementation Plan

**Goal:** 按 RELEASE_GUIDE.md 验证、提交并发布当前代码，生成 v4.2.8 Windows 安装包。

**Architecture:** 沿用现有版本、更新说明与 scripts/publish-release.js；纳入当前工作区礼物姬和横幅改动，只修复发布阻塞。

**Tech Stack:** Node.js 24、Electron 43、npm、Git、GitHub CLI、electron-builder。

## Current Behavior / Ownership / Compatibility

当前 main 为 a953b72（v4.2.7），远端一致，v4.2.8 尚不存在。package.json 拥有版本与构建配置，UPDATE.md 提供 Release 正文，发布脚本校验干净工作区、tag 和附件摘要。保留用户修改、旧导出目录、任务冻结配置及 Electron 权限限制，不操作真实用户数据库、不部署服务器、不改变发布设施。

## Milestones / Verification

- [x] 对比 v4.2.7，更新 package.json、package-lock.json 和 UPDATE.md 至 4.2.8。
- [x] 使用既有固定服务器检出运行 npm run verify；失败时定位归属、做最小修复并重跑相关检查。
- [x] 审阅最终差异，运行 git diff --check、git status --short，确认无运行数据、凭据或生成产物进入提交；记录结果并归档准备计划。

## Release / Failure Handling / Done When

按 guide 提交 v4.2.8，检查无 Claude Co-Authored-By 后推送 main，运行 npm run release:win。核对远端 tag 对应发布提交、三个附件上传状态和摘要、latest.yml 与本地安装包版本及 SHA-512。失败保留现场，修复阻塞后重试，不移动已发布 tag、不覆盖用户改动。准备阶段完成后归档；发布结果单独报告。

## Results

- 首轮契约 5 个夹具、文档 5/5、908 个 JavaScript 语法及架构 22/22 通过。全套测试出现旧导出尺寸断言（1120 × 192，当前为 1008 × 144）和 bilibili-room-profile 测试进程无详细诊断的失败。
- 仅同步 test/fixtures/desktop-overlay-probe.cjs 的窗口、返回尺寸和截图区域，保留真实 Electron 隔离、凭据、图片及截图检查。两处受影响文件独立重跑 6/6 通过；房主资料测试未修改，继续完整重跑确认。
- 最终 npm run verify 全部通过：5 个锁定服务器夹具、文档 5/5、908 个 JavaScript 语法、架构 22/22；npm test 共 2487 项，2483 通过、0 失败、4 项既有 NSIS 集成测试因未配置测试环境跳过。房主资料测试在完整重跑中通过，首轮进程失败未复现。
- 验证前后待发布文件指纹一致。完整日志位于系统临时目录 lira-v4.2.8-verify-final.log；版本、更新说明、尺寸测试修复与现有功能差异已复核，构建和运行数据不进入提交。准备阶段完成，提交与发布随后执行，结果单独报告。
