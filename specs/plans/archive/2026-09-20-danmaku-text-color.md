# 弹幕正文颜色参数

## Goal / Current behavior
九种样式均使用独立的正文颜色，可开放单个颜色参数。新增“弹幕文字颜色”，按样式预览、保存、回读和恢复默认。

## Ownership / Compatibility
沿用管理页参数草稿、客户端/服务器共享 styleOptions 合同与基础渲染 CSS；服务器的 Device/Management/Public OpenAPI 同步扩展。textColor 为可选六位十六进制颜色；省略时沿用主题，旧数据无需迁移。只影响非礼物弹幕正文，昵称、身份、礼物、阴影和背景不变。无鉴权、生命周期或架构变更，不发布。

## Steps / Verification
- [x] 管理页加颜色选择器，共享合同记录各主题默认正文颜色并校验 textColor。
- [x] 双方渲染接入自定义色，更新需求、验收、协议及合成 fixture。
- [x] 聚焦测试覆盖颜色校验、按样式草稿/保存/重置、服务器租户隔离；现有 OBS Playwright 验证九种样式和身份、非正文配色不变。

## Done when
默认不改外观，修改只在应用后影响直播，重置恢复主题颜色；双方检查通过、diff 无无关新增改动。回滚仅撤销本任务增量。

## Verification — complete
客户端 27 项聚焦测试通过（参数合同、管理页、草稿、IPC、CSS 归属）；服务器合同/协议/文档 24 项与设置服务 2 项通过。既有 OBS Playwright 测试扩展覆盖九种样式、五种身份的默认颜色、自定义色、重置与新弹幕，验证昵称/身份/礼物配色和正文阴影不变，通过。测试使用隔离合成状态；未操作用户账号、运行中的桌面应用或生产服务器，未部署。
