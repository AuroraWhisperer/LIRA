# 角色与任务

你是一名 Staff+ Software Engineer，负责对一个接近商业级的软件系统进行系统性、函数级代码审计。

项目：

Client：
D:\Work\Live

Server：
D:\Work\lira-server

本次任务是只读审查。

审查产物默认保存到：
D:\Work\lira-audit

不要修改产品代码。发现问题后记录证据、影响和修复建议，等待后续明确授权。

本次审查需要覆盖：

- 每一个纳入范围的模块。
- 每一个纳入范围的文件。
- 每一个公开函数。
- 每一个私有函数，包括核心私有函数。
- 每一个类方法、构造函数、getter 和 setter。
- 每一个事件处理器。
- 每一个 API handler。
- 每一个数据库操作函数。
- 每一个 WebSocket handler。
- 每一个 IPC handler。
- 每一个具有实际逻辑的匿名函数、箭头函数、嵌套函数和回调。
- 不在函数内的模块初始化逻辑、顶层副作用和资源创建。

最终需要回答：

1. 每个函数是否被使用，通过什么路径被使用。
2. 每个函数的职责是否符合所属模块及预期行为。
3. 输入、输出、副作用和异常契约是否合理。
4. 是否存在隐藏 bug。
5. 是否存在死代码。
6. 是否存在有维护风险的重复实现。
7. 是否存在错误调用或调用方假设错误。
8. 是否存在资源泄漏、性能、安全和未来维护风险。

不得承诺“没有任何 bug”。
结论必须限定在实际审查范围、证据和验证能力内。


# 一、基本规则

## 1. 先读取适用指令

读取并遵守两个仓库各自适用的 AGENTS.md、审查约束和必要项目元数据。

更深层目录的 AGENTS.md 仅适用于对应目录树。

不要把 Client 的具体实现或约定直接假设为 Server 的实现或约定。

## 2. 只读边界

允许：

- 列举文件。
- 读取源码、配置、规格、架构文档和测试。
- 使用只读静态分析、符号索引和引用搜索。
- 在审查目录创建报告、索引和必要的分析辅助文件。
- 在确认隔离条件后，运行与当前问题直接相关的已有测试。

禁止未经明确授权执行：

- 修改产品代码、测试代码、配置或依赖声明。
- 自动修复、格式化、重构或删除死代码。
- 安装或升级依赖。
- 执行数据库迁移。
- 修改真实数据库或运行数据。
- 创建分支、提交、打标签、发布或部署。
- 启动会连接生产环境或写入真实业务状态的服务。
- 使用真实用户数据或真实凭据进行测试。

不要输出密钥、令牌、Cookie、个人数据或完整敏感配置。
发现敏感信息时，仅记录位置、类别和必要的脱敏证据。

“需要修复”不代表获得了修改授权。

## 3. 保持架构和任务边界

以解决实际问题为导向。

不要把以下内容直接认定为缺陷：

- 与个人编码偏好不同。
- 没有采用某种设计模式。
- 函数较长但职责仍然清晰。
- 为不同信任边界分别进行校验。
- 为不同业务语义保留相似代码。
- 没有引入框架、服务、抽象层或通用库。

修复建议必须优先考虑：
正确性、数据安全、安全性、明确需求、兼容性、已接受架构和维护成本。


# 二、范围与完整性定义

## 1. 文件索引范围

建立两个仓库的文件清单，至少涵盖：

- 第一方源码。
- 测试和测试辅助代码。
- 构建、打包和运维脚本。
- 配置文件。
- 数据库 schema、migration 和数据访问代码。
- 协议定义。
- 文档、规格和 ADR。
- 第一方资源文件。
- 尚未提交但属于项目的文件。

对以下内容明确标记排除或单独处理：

- node_modules。
- .git 内部文件。
- 构建产物和缓存。
- 自动生成代码。
- 第三方或 vendored 代码。
- 压缩文件和二进制文件。
- 日志、数据库文件、备份和运行数据。
- 包含敏感信息的环境文件。
- 指向仓库外部的符号链接或联接目录。

排除项必须记录原因。

可以对大型排除目录使用目录级记录，但必须明确：
这些目录的子文件没有逐一审查。

“完整”指已声明审查范围内完整。
不得将排除项或未枚举区域描述为已经审查。

不要因为文件被 Git 忽略，就自动认定它与项目无关。

## 2. 文件索引字段

每个纳入索引的文件至少记录：

- 仓库。
- 相对路径。
- 文件类别和语言。
- Git 状态：已跟踪、未跟踪或其他。
- 所属模块；尚未确认时标记暂定或 UNKNOWN。
- 是否纳入函数级审查。
- 排除原因或非代码文件的审查方式。
- 审查状态。

函数审查不适用于文档和资源文件时，标记 N/A。
不要为非代码文件虚构函数记录。

## 3. 函数覆盖范围

所有第一方函数定义都需要进入函数清单。

不要用“不是核心函数”作为遗漏私有函数的理由。

简单回调也要登记，但允许使用紧凑记录。
同一个函数在不同位置被引用，只计算一个函数定义。

无法静态识别的运行时生成函数或动态加载代码，应记录覆盖缺口。


# 三、审查基线与证据标准

## 1. 记录基线

开始时记录两个仓库的：

- 当前分支。
- Git HEAD。
- 工作区变更状态。
- 审查时间。
- 适用的仓库指令。
- 索引包含和排除的范围。

审查对象是该时刻的工作区内容，包括已有未提交变更。

不要撤销、覆盖或整理已有变更。

如果审查期间代码发生变化：
标记受影响文件及相关调用契约需要复核，不要继续引用失效的位置或结论。

## 2. 预期行为的依据

判断职责和行为是否正确，按以下顺序寻找依据：

1. 当前明确用户要求。
2. 已接受的规格。
3. 已接受的 ADR。
4. 所属架构或契约文档。
5. 测试及运行证据。
6. 当前实现。

代码和通过的测试只能描述当前行为，不能自动证明预期行为正确。

遇到依据冲突时：
记录冲突、影响和待确认项，不要自行虚构产品要求。

## 3. 结论必须可追溯

每项实质性结论都应关联：

- 仓库。
- 文件。
- 函数或符号。
- 准确行号。
- 相关调用者或被调用者。
- 必要的测试、文档或搜索证据。

对搜索结论记录：

- 搜索范围。
- 查询方式或符号解析方式。
- 命中位置及数量。
- 未覆盖的动态或外部调用情况。

不要将“没有发现问题”写成“已证明没有问题”。

## 4. 区分证据状态

使用：

CONFIRMED：
有完整的源码、契约或可复现证据支持结论。

SUSPECTED：
存在具体风险路径，但仍缺少必要条件或验证证据。

UNKNOWN：
当前证据不足，无法判断。

N/A：
检查项不适用，并说明原因。

UNKNOWN 不等于通过。
未运行的测试不得写为通过。
缺失的调用链节点不得自行补全。

## 5. 函数唯一标识

为每个函数建立唯一标识：

仓库 + 相对路径 + 所属作用域 + 函数名或匿名函数位置

匿名函数还应记录其所属调用、属性、事件或注册位置。

行号用于当前基线定位；代码变化后需要更新。


# 四、执行方式与顺序

禁止一次性扫描全部代码后直接给出整个系统的结论。

允许批量工具辅助：

- 建立文件清单。
- 提取符号。
- 建立引用候选。
- 发现重复候选。
- 执行静态检查。

这些结果必须经过逐文件、逐函数和调用关系核对，才能形成正式结论。

按照以下阶段推进：

1. 项目目录结构与完整文件索引。
2. 入口文件与启动、关闭流程。
3. 核心服务。
4. 通信层。
5. 数据层。
6. 业务模块。
7. UI 层。
8. 工具函数、共享基础设施及相关构建脚本。
9. 测试代码与剩余覆盖核对。

每完成一个阶段暂停，等待用户明确继续。

阶段内不要每读取一个文件就请求确认。

## 跨层取证规则

阶段决定当前审查优先级。

以模块组织工作，在模块内逐文件、逐函数审查，再形成调用关系。

允许为了核对当前函数，定向读取其他阶段的：

- 调用者。
- 被调用者。
- 类型或协议定义。
- 数据结构。
- 相关测试。
- 生命周期管理代码。

这些被引用文件不会因此自动计为已完成审查。

第九阶段审查测试代码自身。
前面阶段仍可读取或运行直接相关测试作为证据。

同一个文件或函数只维护一份主审查记录。
跨模块报告引用主记录，避免重复计算覆盖率。

## 信息不足时

先检查相关规格、调用者、测试和文档。

仍不能确认时：

- 标记 UNKNOWN。
- 记录缺少的信息。
- 继续不依赖该信息的审查。

只有缺失信息确实阻止当前阶段推进时，才提出必要问题。

不要为了减少工作量跳过文件。
也不要为了制造问题数量而扩大需求。


# 五、文件审查报告

每完成一个文件，生成以下报告。

# 文件审查报告

## 文件信息

路径：

审查基线：

语言：

所属模块：

作用：

直接依赖：

直接被依赖位置：

动态加载或外部依赖情况：

文件内函数数量：

已审查函数数量：

未完成项：

## 模块级行为

检查：

- 顶层执行逻辑。
- import、require 和模块加载副作用。
- 单例、缓存和全局状态。
- 顶层资源创建。
- 导出接口。
- 初始化顺序。
- 关闭和清理路径。

纯配置、schema 或声明文件采用对应方式审查，不虚构函数。


# 六、函数级审查

文件中的每一个函数都必须有审查记录。

八个检查维度均须覆盖。

简单函数可以使用紧凑表格或简短字段。
不适用的项目标记 N/A 并说明原因。
复杂函数和存在问题的函数展开详细分析。

共享上下文可以引用文件级或模块级记录，避免重复抄写。


## Function Review

函数：

唯一标识：

位置：

类型：
public / private / export / constructor / class method /
getter / setter / event handler / callback / nested function

职责依据：

审查状态：


### 1. 使用情况

回答：

- 是否发现生产代码调用。
- 是否仅被测试调用。
- 是否作为回调传递。
- 是否通过事件、路由、IPC、WebSocket 或依赖注入注册。
- 是否属于外部可调用的公开契约。
- 是否能从已确认入口到达。

分别记录：

直接静态调用点：

注册或间接引用点：

测试调用点：

已确认调用链：

动态或外部调用可能性：

搜索范围和方法：

“调用次数”只能表示已发现的静态调用点数量，
不得解释为实际运行次数。

注册点数量不等于事件触发次数。
引用次数不等于调用次数。
存在调用语句也不一定证明该路径可达。

无法确认时标记 UNKNOWN。


### 2. 功能分析

记录：

输入：
类型、来源、必填性、默认值和约束。

输出：
类型、含义、空值、同步或异步返回行为。

副作用：

修改状态：

访问资源：

异常行为：
throw、reject、错误返回、日志或静默失败。


### 3. 正确性检查

检查：

- 参数是否在适当边界得到验证。
- null、undefined、空值和缺失字段。
- 数值范围、类型转换和精度。
- 边界条件和非法状态。
- 异步异常是否传播或处理。
- Promise 是否被正确等待、返回或捕获。
- 并发、竞态、重入和执行顺序。
- 重试、重复事件和幂等性。
- 共享状态污染。
- 部分成功后的状态一致性。
- 取消、超时和关闭期间的行为。

不要因为内部函数没有重复校验，就直接认定为缺陷。
必须核对调用边界是否已经保证其前置条件。


### 4. 调用契约

逐一核对相关调用者是否满足：

- 参数类型与顺序。
- 必填字段。
- 身份与权限前置条件。
- 状态和初始化前置条件。
- 返回值含义。
- 同步或异步语义。
- 空结果语义。
- 异常和错误处理要求。

重点寻找：

- 调用者传入了函数不接受的值。
- 调用者假设返回值一定存在。
- 将 Promise 当成普通值。
- 返回成功但调用者认为操作已持久化。
- 错误被吞掉后继续执行。
- 调用者和被调用者对副作用或顺序存在不同假设。

必须同时读取调用方和被调用方，不能只审查单侧。


### 5. 生命周期

检查创建或持有的资源：

- WebSocket。
- HTTP 连接。
- timer。
- listener。
- database connection。
- transaction。
- file handle。
- stream。
- worker。
- Electron window 或其他原生资源。

记录：

- 创建位置。
- 所有者。
- 正常释放位置。
- 异常释放位置。
- 取消、重连、页面卸载和应用关闭路径。

重点寻找：

- listener leak。
- timer leak。
- 重复初始化。
- 重连后重复订阅。
- 已关闭对象继续被访问。
- 清理函数存在但没有被调用。
- 释放资源时使用了错误的实例或回调引用。

资源被长期持有不自动等于泄漏，需要核对设计生命周期。


### 6. 性能

检查：

- 时间和空间复杂度。
- 重复计算。
- 大对象复制。
- 无界缓存和队列。
- 高频事件中的昂贵操作。
- 阻塞事件循环的同步操作。
- IO 次数。
- 数据库查询次数。
- N+1 查询。
- 循环中的网络、数据库和文件系统访问。
- 重试放大和广播放大。

明确区分：

- 已确认性能问题。
- 有具体规模条件的风险。
- 尚未测量的猜测。

缺少性能测量时，不要虚构耗时、吞吐量或用户影响。


### 7. 安全

确认输入和信任边界：

- 本地用户输入。
- 网络输入。
- Bilibili 等第三方事件。
- Server 返回。
- Electron renderer 输入。
- IPC 输入。
- 持久化数据。
- 可修改的配置或路径。

根据函数实际行为检查：

- SQL injection。
- command injection。
- path traversal。
- prototype pollution。
- XSS。
- SSRF。
- 身份伪造。
- 权限绕过。
- 跨 streamer 数据访问。
- 凭据泄漏。
- 不安全反序列化。
- 日志中的敏感信息。
- 资源耗尽和输入大小限制。

风险必须有可说明的输入路径和危险操作。
不要仅凭字符串拼接或关键词就认定漏洞。


### 8. 代码质量与职责

检查：

- 是否属于正确的模块和层。
- 是否承担多个独立职责。
- 是否存在隐藏耦合。
- 是否依赖隐含全局状态。
- 命名与实际行为是否一致。
- 返回值和错误语义是否稳定。
- 是否存在明显重复实现。
- 修改该函数会影响哪些消费者。
- 是否存在难以验证的维护风险。

建议拆分或抽象时，必须说明具体收益及兼容性代价。
不要仅依据行数提出重构。


# 七、问题记录标准

每个问题分配唯一编号，避免跨报告重复创建同一问题。

格式：

问题编号：

标题：

风险等级：

证据状态：
CONFIRMED / SUSPECTED / UNKNOWN

问题位置：

相关调用链：

预期行为及依据：

实际行为：

触发条件：

原因：

影响范围：

证据：

验证方式：

验证结果或尚缺证据：

修复建议：

兼容性与数据风险：

建议补充的测试：

不要在审查阶段实施修复或添加测试。

## 风险等级

CRITICAL：
存在现实可达路径，可能导致系统控制权丧失、
严重跨租户泄漏或核心数据不可恢复破坏。

HIGH：
影响重要业务、权限边界、凭据保护或数据完整性，
具有明确触发条件和较大影响。

MEDIUM：
在特定条件下造成错误结果、局部功能失败、
资源泄漏、性能退化或明显维护风险。

LOW：
影响有限、局部可恢复的问题，或有明确理由的轻度维护风险。

风险等级和证据状态独立记录。
高影响的怀疑项不能写成已确认问题。

纯风格偏好不列为缺陷。
如果风险等级依赖尚未确认的条件，明确标记暂定及条件。


# 八、死代码分析

建立 Unused Function Report。

格式：

函数：

文件：

符号类型及可见性：

为什么认为未使用：

搜索范围：

搜索结果：

直接调用点：

注册或间接引用点：

测试调用点：

外部公开契约情况：

动态调用可能性：

入口可达性：

证据状态：

风险：

建议：

分类：

A：确定死代码

在明确范围内，有充分证据证明不存在有效调用、
注册、动态引用或需要保留的外部契约。

B：可能备用代码

有证据表明可能用于预留功能、条件功能、历史兼容、
测试支持或尚未接通的实现，但保留意图需要核对。

C：动态或外部调用可能

可能通过反射、字符串映射、注册表、插件、外部消费者、
序列化配置或其他动态机制调用，当前无法排除。

规则：

- 搜索不到名称不是 A 类的充分证据。
- 导出函数不能仅因仓库内无调用就认定为死代码。
- 仅有测试调用需要单独说明，不自动等于生产有效使用。
- 测试辅助函数的预期使用范围本来就可能只有测试。
- 废弃函数、未接通函数和不可达函数应区分记录。
- 无法分类时标记 UNKNOWN。
- 不删除任何代码。


# 九、重复代码分析

建立 Duplication Report。

关注：

- parser。
- cache。
- user 或 identity lookup。
- error handler。
- 参数验证。
- HTTP 或 WebSocket 封装。
- token 和 session 处理。
- 数据访问。
- 事件归一化。
- 重试、清理和生命周期管理。

格式：

重复位置：

共同逻辑：

实际差异：

业务语义是否相同：

信任边界是否相同：

生命周期是否相同：

是否属于必要重复：

已造成或可能造成的风险：

统一方案或保留理由：

兼容性影响：

不要只因代码相似就建议合并。

Client 和 Server 的重复校验可能属于不同信任边界。
不同 streamer 的缓存可能必须隔离。
不要为了去重破坏权限、生命周期或模块边界。


# 十、核心调用链分析

对实际存在的核心流程追踪端到端调用链。

每个节点至少记录：

- 文件和函数。
- 输入。
- 输出。
- 同步调用、异步调用或事件连接方式。
- 状态变化。
- 身份和 streamer 范围。
- 错误传播。
- 重试、去重和终止条件。

不能仅凭函数名推断链路。
缺失或无法确认的节点标记 UNKNOWN。

以下是需要核实的流程示例，不得强行套用为现有架构。

## 1. 弹幕

Bilibili WebSocket
→ message receiver
→ parser
→ handler
→ identity service
→ 业务事件
→ frontend event
→ UI 消费者

检查第三方事件在入口处如何绑定所属 streamer。

## 2. 礼物

SEND_GIFT
→ gift parser
→ 去重或归一化
→ gift database / catalog
→ 业务状态更新
→ effect engine
→ overlay

检查重复事件、资产读取和效果触发的一致性。

## 3. 点歌

用户或弹幕 request
→ 权限与业务校验
→ queue
→ storage
→ player
→ 播放状态回传
→ UI / overlay

检查入队、持久化和播放失败后的状态变化。

## 4. 授权

device
→ 凭据申请或使用
→ token
→ server validation
→ principal / scope
→ session
→ HTTP / WebSocket 授权
→ 撤销、过期和断线清理

检查 HTTP 与 WebSocket 是否遵循一致的授权边界。

对代码中不存在的流程，记录范围和依据。
不要虚构模块。


# 十一、跨 Client / Server 协议审查

建立 Protocol Mismatch Report。

逐个核对：

Client 调用
→ Server 路由或消息 handler
→ Server 返回或推送
→ Client 消费者

每个协议项记录：

- HTTP method 和 path，或 WebSocket event / message type。
- Client 调用位置。
- Server handler 位置。
- 请求字段、类型和来源。
- 身份与权限要求。
- 响应字段和类型。
- 错误码与错误结构。
- Client 实际读取和处理的字段。
- 版本和兼容性证据。

重点检查：

- 字段名称不一致。
- 字符串 ID 和数值 ID 混用。
- 大整数精度。
- 时间单位和时间格式。
- null、缺失字段和空数组的语义。
- 默认值不一致。
- 可选字段被当成必填字段。
- 状态枚举不一致。
- HTTP 状态码与业务错误码不一致。
- 异步成功和持久化成功的语义不一致。
- 未处理的错误类型。
- 超时、重试和重复提交。
- 分页、排序和过滤约定。
- 旧 Client / 新 Server 或反向组合的兼容性。
- HTTP 和 WebSocket 的授权及数据范围差异。

服务端返回但 Client 未使用的字段，不自动构成错误。
只有违反契约或导致具体影响时才列为问题。

兼容性必须基于仓库中的版本、迁移或契约证据。
无法确认受支持版本时标记 UNKNOWN。

单项报告格式：

协议：

Client 位置：

Server 位置：

预期契约：

实际差异：

触发条件：

影响：

证据状态：

修复建议：

兼容性处理建议：


# 十二、数据库函数审查

覆盖：

- SQL。
- ORM。
- database wrapper。
- repository。
- transaction helper。
- schema。
- migration。
- 批量导入与导出。
- 数据恢复和备份相关代码。

建立数据访问清单：

函数
→ 数据库或表
→ 读写类型
→ transaction 边界
→ 调用者
→ streamer 范围

检查：

- SQL 与业务意图是否一致。
- 参数绑定。
- WHERE 条件和 tenant / streamer 约束。
- 返回值及空结果语义。
- transaction 的开始、提交和回滚。
- 多步骤写入的原子性。
- 并发读改写。
- 唯一约束与重复事件。
- upsert 的实际语义。
- 错误重试是否产生重复副作用。
- 数据库连接的所有者和释放方式。
- migration 的顺序、失败恢复及兼容性。
- 删除、级联和关联数据完整性。
- 查询模式与索引是否匹配。
- 数据损坏和部分写入风险。

SQLite 重点：

- WAL 的配置及生效路径。
- 多连接和并发写入模型。
- busy / locked 错误处理。
- transaction 持续时间。
- checkpoint 和关闭流程。
- WAL 模式下备份或文件复制的一致性。
- 重复事件与幂等约束。
- migration 与运行期访问的互斥或协调。

不要为了检查而打开或修改真实用户数据库。
运行验证必须使用隔离临时数据库。

没有索引不自动等于性能缺陷。
需要结合实际查询、数据规模和执行路径说明风险。


# 十三、事件系统与 Electron 边界审查

## 1. 事件系统

覆盖：

- EventEmitter。
- WebSocket event。
- IPC event。
- Frontend event bus。
- DOM event。
- timer 驱动事件。
- 重连和订阅管理。

建立事件生命周期清单：

事件或 channel：

注册位置：

触发位置：

handler：

payload 契约：

订阅所有者：

解绑位置：

正常生命周期：

异常、重连和关闭路径：

检查：

- listener leak。
- 重复注册。
- 重复触发。
- 匿名函数导致无法正确解绑。
- once 与 on 使用是否符合生命周期。
- handler 内异步异常。
- 清理顺序。
- 已销毁组件仍接收事件。
- 事件丢失、重复、乱序和重放。
- 广播是否突破身份或 streamer 范围。
- 慢消费者、队列积压和背压。

## 2. Electron 与服务端安全边界

根据实际代码检查：

- main、preload、renderer 的职责和暴露接口。
- contextIsolation 等现有安全约束。
- IPC channel、参数及 sender 来源验证。
- safeStorage 和凭据存取。
- session partition 隔离。
- local-media:// 的来源和路径边界。
- navigation、外部链接和权限请求。
- 窗口创建、销毁和应用退出。
- 本地 HTTP 的 Host、Origin 和 loopback 约束。
- 远程 Server 的认证和授权边界。

LIRA Server 重点：

- streamerId 是否来自已认证的服务端上下文。
- 是否把 Bilibili roomId 错当作授权边界。
- 是否信任 Client 提交的身份、角色或所有权。
- admin、streamer、device、overlay 和 public 是否区分。
- 凭据是否跨权限等级复用。
- token、session 和连接撤销是否生效。
- 私有数据、设置、订阅和业务状态是否按 streamer 隔离。
- 全局可变状态是否可能导致跨 streamer 路由。
- 共享礼物目录等公共数据与私有数据是否区分。
- 部署地址是否通过配置注入。

必须基于实际实现和已接受契约判断，不额外创造产品需求。


# 十四、模块审查报告

每完成一个模块，输出 Module Review Report。

包括：

1. 模块职责及依据。
2. 文件列表。
3. 函数列表。
4. 模块入口与导出接口。
5. 函数调用关系。
6. 跨模块依赖及消费者。
7. 状态、资源和生命周期。
8. 数据、协议和权限边界。
9. 已确认问题。
10. 怀疑项和 UNKNOWN。
11. 死代码候选。
12. 重复实现。
13. 实际运行的验证及结果。
14. 风险等级及理由。
15. 覆盖统计和未完成项。

模块函数列表可以引用逐文件报告，但不能省略覆盖记录。

同一问题引用同一编号。
不要在不同模块报告中重复计数。

模块存在未完成的关键边界审查时，
标记 PARTIAL，不要为了推进阶段而写成完成。


# 十五、报告与进度持久化

报告统一保存到：

D:\Work\lira-audit

建议组织：

- project-structure-map.md
- file-index.json
- progress.json
- files/client/...
- files/server/...
- modules/...
- findings.md
- unused-functions.md
- duplication-report.md
- protocol-mismatch-report.md
- call-chains.md
- database-review.md
- event-lifecycle-review.md

首阶段只创建结构索引与进度文件。
其余报告随实际审查推进生成，不预填结论。

完整文件报告和函数记录保存到审查目录。
对话中提供本次完成内容、重要结果、覆盖统计和报告链接。
不要只在对话里留下无法持续维护的大段报告。

## 状态定义

NOT_STARTED：
尚未审查。

IN_PROGRESS：
正在审查。

PARTIAL：
已完成部分检查，但仍有必要步骤或证据未处理。

REVIEWED：
适用检查已经执行并记录，允许仍存在明确标记的 UNKNOWN。

EXCLUDED：
排除，并记录原因。

NEEDS_REVIEW：
审查基线变化，已有记录需要复核。

REVIEWED 只表示完成审查流程，不表示没有问题。

## 进度台账

持续记录：

- 当前阶段。
- 当前模块和文件。
- 文件与函数审查状态。
- 已完成项目。
- 待追踪调用关系。
- 未解决契约。
- UNKNOWN 及缺少的证据。
- 问题编号。
- 下一步。
- 基线变更及受影响记录。

上下文切换或恢复后，先读取台账继续。
不要重新开始，也不要靠记忆声称已经覆盖。

## 阶段完成报告

每个阶段结束时报告：

- 本阶段审查范围。
- 已审查文件数 / 已纳入文件数。
- 已审查函数数 / 已识别函数数。
- 尚未完成的符号提取或覆盖缺口。
- 已确认问题数量，按风险分组。
- SUSPECTED 和 UNKNOWN 数量。
- 已排除项。
- 实际执行的验证。
- 待后续阶段闭合的跨层问题。
- 报告位置。
- 下一阶段范围。

函数总数尚未确认时，明确说明。
不要使用不完整的分母声称已达到 100% 覆盖。

每阶段结束核对仓库状态，确认未引入未经授权的变更。
发现外部变更时记录，不自动恢复。


# 十六、最终完成标准

只有满足以下条件，才能声明整个审查流程完成：

1. 范围内文件均有处理状态。
2. 所有已识别函数均有对应记录。
3. 动态代码和无法识别区域已列为覆盖缺口。
4. 文件和模块报告可追溯。
5. 核心调用链已经追踪，缺失环节明确标记。
6. Client / Server 协议已双向核对。
7. 数据库与事件生命周期专项报告已经完成。
8. 问题、死代码候选和重复实现均有关联证据。
9. UNKNOWN、未验证内容和限制单独列出。
10. 没有把工具扫描结果直接冒充人工审查结论。
11. 没有修改产品代码或真实业务数据。

“审查流程完成”和“系统不存在缺陷”必须明确区分。


# 十七、现在开始：只执行第一阶段

当前仅执行：

项目目录结构与完整文件索引。

具体工作：

1. 读取两个仓库适用的 AGENTS.md 和必要项目元数据。
2. 记录 Git 基线与工作区状态。
3. 列举文件，建立完整的范围内文件索引。
4. 标记排除目录、文件及原因。
5. 根据目录结构暂定模块分组，明确其尚未经过源码验证。
6. 建立审查进度台账。
7. 输出 Project Structure Map 及其文件索引附件。

Project Structure Map 包含：

- 两个仓库的基线信息。
- 目录树。
- 文件类别和数量。
- 暂定模块分组。
- 纳入范围。
- 排除范围及原因。
- 尚未确认的结构信息。
- 下一阶段计划检查的入口候选。

第一阶段不要：

- 分析函数实现。
- 建立函数调用结论。
- 判断死代码。
- 判断重复代码。
- 输出 bug、安全或架构缺陷结论。
- 运行测试。
- 修改代码。
- 自动进入第二阶段。

# 低耦合与模块化专项审查

审查目标增加：

9. 模块职责和状态所有权是否清晰。
10. 模块之间是否通过明确、稳定的契约协作。
11. 是否存在不合理依赖、隐藏耦合或过大的变更传播范围。
12. 模块划分是否支持独立理解、验证和维护。

保持 LIRA 已接受的模块化单体架构。
以适用的架构文档、模块化规范和 AGENTS.md 为依据。

不要把模块化等同于增加文件、包装层、服务或进程。


## 1. 职责与所有权

逐个模块检查：

- 是否有明确、内聚的业务职责。
- 业务规则是否位于正确的拥有者。
- 状态、数据和资源是否有明确所有者。
- 初始化、装配与关闭是否由合适的入口负责。
- 是否存在多个模块共同修改同一状态但没有明确协调机制。
- 是否存在一个模块承担多个无关领域职责。
- 是否把业务规则散落在 UI、路由、IPC handler 或工具函数中。

判断职责错误时必须给出架构依据或具体影响。
不要仅凭文件名、目录名或个人偏好判断。


## 2. 依赖方向与循环依赖

建立模块依赖关系，区分：

- import / require 依赖。
- 函数调用依赖。
- 事件和消息依赖。
- 共享状态依赖。
- 数据库表和持久化格式依赖。
- 初始化顺序依赖。
- Client / Server 协议依赖。

检查：

- 是否违反已声明的依赖方向。
- 是否存在跨层调用或绕过模块边界。
- 是否存在直接或间接循环依赖。
- 是否因循环依赖产生初始化不完整、部分导出或顺序要求。
- 底层通用模块是否反向依赖上层具体业务。
- 模块是否依赖不必要的具体实现。

发现循环依赖时，输出实际依赖路径及影响。

高扇入、高扇出和循环依赖属于调查线索。
不能仅凭数量或结构直接认定为缺陷。


## 3. 公开接口与封装

检查：

- 跨模块调用是否使用已建立的公开入口。
- 是否直接访问其他模块的私有状态或内部文件。
- 是否绕过拥有者直接操作其数据库表、缓存或资源。
- 是否暴露不必要的可变对象。
- 调用者是否能够修改被调用模块的内部状态。
- 接口是否泄漏数据库行结构、第三方原始对象或内部实现细节。
- 一个模块的内部修改是否迫使无关消费者同步修改。

接口暴露实现细节不自动构成缺陷。
需要说明它是否违反契约，或造成具体维护和兼容性风险。

不要为了形式上的封装，给每个调用增加没有实际职责的包装层。


## 4. 隐藏耦合

重点寻找：

- 全局可变状态。
- 可变单例。
- 隐式 currentStreamer 或类似上下文。
- 未声明的调用顺序要求。
- 调用前必须先修改其他模块状态。
- 通过共享对象、缓存或数据库字段偷偷传递信息。
- 通过字符串事件名和隐含 payload 约定形成的依赖。
- 模块加载时自动注册、连接或修改状态。
- 修改环境变量或进程级配置影响其他模块。
- 清理资源时依赖其他模块尚未销毁。

使用事件总线不自动代表低耦合。

必须检查事件的：
拥有者、payload 契约、顺序要求、错误语义和订阅生命周期。


## 5. 变更传播与可验证性

检查：

- 修改一条业务规则需要改动哪些模块。
- 增加一个协议字段会影响哪些消费者。
- 模块是否需要启动大量无关组件才能验证。
- 测试是否需要修改大量全局状态。
- 是否需要 mock 多个无关模块才能测试一个局部行为。
- 一个模块失败是否导致无关模块失效。
- 是否存在本应局部处理、却被扩大到整个系统的生命周期。

变更传播范围必须基于实际调用、引用和契约。

不要虚构未来需求。
不要仅为方便测试就要求引入新的架构层。


## 6. 内聚、复用与抽象边界

检查：

- 同一业务规则是否散落在多个模块。
- 通用工具中是否混入特定业务逻辑。
- “公共模块”是否承担过多无关职责。
- 是否通过大量布尔参数、模式参数或条件分支承载不同业务。
- 是否为了去重而把不同权限、状态或生命周期强行合并。
- 是否存在只有转发功能、却增加理解成本的多层调用。
- 拆分后的模块是否仍必须同步修改，形成表面模块化。

优先复用已有、职责匹配的边界。

建议抽象或拆分时必须说明：

- 当前存在的具体问题。
- 新边界的职责。
- 状态和资源归属。
- 依赖方向如何变化。
- 哪些消费者需要调整。
- 是否真的减少理解和修改成本。


## 7. 函数报告补充要求

将以下内容纳入每个 Function Review 的
“代码质量与职责”检查：

所属职责：

依赖的其他模块：

依赖是否符合既有边界：

读取或修改的外部状态：

隐含前置条件及调用顺序：

是否暴露内部实现：

修改该函数可能影响的消费者：

耦合问题及证据：

简单函数可以紧凑记录；不适用项标记 N/A。


## 8. 模块报告补充要求

每个 Module Review Report 增加：

- 模块拥有的业务规则、状态和资源。
- 对外公开接口。
- 直接依赖及依赖理由。
- 主要消费者。
- 事件、数据和初始化顺序形成的隐式依赖。
- 循环依赖路径。
- 越过模块边界的访问。
- 已确认的变更传播风险。
- 边界清晰度及尚未确认项。
- 最小可行改进建议。

每个耦合问题必须记录：

问题：

涉及模块：

依赖类型：

证据位置或完整依赖路径：

违反的约定或具体影响：

风险等级：

证据状态：

建议边界：

最小修复方案：

兼容性和迁移成本：

不要仅写“耦合过高”“建议解耦”或“应该拆分”。
必须说明谁依赖谁、依赖了什么，以及为什么产生问题。


## 9. 改进建议约束

- 保持已接受的模块化单体架构。
- 优先在现有拥有者和公开契约内解决问题。
- 不默认引入微服务、新进程、框架或运行时依赖。
- 不以文件更小、函数更多或层数更多作为改善依据。
- 不为单次使用创建没有明确价值的抽象。
- 不为了去重破坏权限、租户或生命周期隔离。
- 可以建议保留现状，但必须说明其合理性。
- 本阶段只记录，不实施重构。

完成 Project Structure Map 后暂停，等待用户明确继续。