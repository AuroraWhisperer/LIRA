# Bilibili 直播弹幕点歌插件项目需求与工程计划

## 1. 结论

可实现的核心目标是：主播电脑本地运行一个低消耗点歌软件，监听 Bilibili 当前直播间弹幕。观众发送 `点歌 歌名` 或 `点歌歌名` 后，软件自动识别弹幕，把歌曲加入本地点歌队列，并通过直播姬加载的直播画面 overlay 显示给观众。

第一版不需要自建服务器，不需要云数据库，不需要域名，不需要每月付费。数据库在主播自己的电脑里，谁直播就用谁电脑里的本地数据库。

## 2. 项目目的

本项目不是做一个公网网站，也不是做收费 SaaS，而是做一个直播辅助软件。

项目目的：

- 降低主播直播时人工看弹幕点歌的成本。
- 让观众能通过弹幕自然点歌。
- 让点歌结果在直播画面里可视化展示。
- 让主播拥有自己的本地歌库、分类和点歌记录。
- 让非技术用户也能简单调整 UI 颜色和样式。
- 尽量低消耗运行，不影响直播姬、游戏、美颜、声卡和其他直播软件。

## 3. 可行性确认

### 3.1 Bilibili 弹幕监听

可行。

Bilibili 开放平台包含直播能力，公开文档中提到直播间消息长连能力。直播间消息长连可用于接收直播间消息，其中弹幕消息包含用户昵称和用户标识等字段。实际开发时优先走官方开放平台能力。

可选接入路线：

- 官方开放平台路线：稳定性和合规性更好，但可能需要开发者身份、应用配置或权限申请。
- 公开直播间 WebSocket 路线：启动体验更简单，但属于非官方开放能力，稳定性和合规风险更高。

第一版建议先预留两种接入方式，实际开发优先验证官方开放平台。

参考：

- [Bilibili 开放平台文档](https://open.bilibili.com/doc)
- [Bilibili 开放平台开发者服务协议](https://open.bilibili.com/agreement/developer-service)

### 3.2 直播间画面展示

可行。

点歌板不会直接嵌进 Bilibili 网页的弹幕区，而是作为直播画面 overlay 被直播姬加载。观众最终是在直播画面里看到点歌板。

示例：

```text
http://127.0.0.1:3000/overlay/queue
http://127.0.0.1:3000/overlay/songs
```

主播在直播姬里添加本地 H5/网页源后，overlay 会被合成到推流画面中。

### 3.3 本地数据库

可行。

使用 SQLite 即可满足歌曲库、分类、点歌队列、历史记录、设置、备份等需求。SQLite 是本地文件数据库，不需要安装 MySQL、MongoDB 或云数据库。

### 3.4 全民 K 歌

部分可行。

可以免费实现：

- 在本地数据库里维护“主播会唱/全民 K 歌可唱歌单”。
- 从 Excel 导入歌曲名、类别、歌手。
- 按分类展示歌曲。
- 观众点歌后，主播复制歌名去全民 K 歌手动搜索。

第一版不承诺：

- 自动控制全民 K 歌客户端。
- 自动搜索全民 K 歌曲库。
- 自动拉取伴奏。
- 自动播放音乐。

这些涉及第三方平台接口、版权、登录态和官方授权，后续应单独评估。

## 4. 硬性约束

### 4.1 不使用自建服务器

系统自己的后端、前端、数据库都运行在主播电脑本地。

不使用：

- 云服务器。
- 云数据库。
- 远程中转后端。
- 需要月费的托管平台。
- 公网域名和备案。

允许使用：

- 主播电脑连接 Bilibili 获取当前直播间弹幕。
- 主播电脑本地启动 `127.0.0.1` 服务。
- 直播姬加载本地 overlay 地址。

### 4.2 数据只存在主播电脑

每个主播使用自己的本地数据库文件。

```text
song-request-data.db
```

数据库保存：

- 主播会唱歌库。
- 歌曲分类。
- 歌曲标签。
- 当前点歌队列。
- 点歌历史。
- UI 主题设置。
- 系统配置。
- 黑名单和冷却设置。

主播换电脑时，通过导出/导入或复制数据库文件迁移。

### 4.3 低消耗

低消耗要求：

- 后端只保持必要的弹幕长连接。
- 不高频轮询接口。
- 不高频写数据库。
- overlay 数据变化时刷新，不做无意义重绘。
- 不下载音乐、不转码、不自动播放。
- 本地日志限制大小。
- 数据库定期备份，但不频繁备份。

预期占用：

- CPU：空闲时接近 0%，有弹幕时短暂处理。
- 内存：目标几十 MB 到一两百 MB。
- 网络：主要是一条弹幕长连接。
- 磁盘：数据库、配置和少量日志。

### 4.4 流畅性和可靠传递

系统需要按“事件驱动 + 本地实时推送”的方式设计，避免卡顿、延迟明显、前端收不到数据等问题。

后端处理方法：

- 弹幕接收使用长连接，不使用高频轮询。
- 弹幕进入后先做轻量解析，只处理与点歌相关的消息。
- 点歌队列优先保存在内存状态中，保证 UI 更新快。
- SQLite 落库使用短事务，避免长时间占用数据库。
- 对连续大量弹幕做限流和去抖，只把必要状态推送给前端。
- WebSocket 推送采用“状态快照 + 事件通知”组合，前端断线重连后可以重新拉取完整状态。
- 写日志异步处理，不阻塞弹幕解析和 UI 推送。
- Excel 导入、大量歌曲索引重建等重任务放到后台任务中执行，不阻塞点歌队列。
- 数据库表建立必要索引，例如歌曲名、拼音首字母、分类、队列状态。

前端处理方法：

- overlay 使用 WebSocket 接收更新，不频繁请求接口。
- overlay 只在队列或歌曲展示数据变化时刷新。
- 歌曲展示滚动使用 CSS 动画或浏览器原生能力，避免 JavaScript 高频定时器。
- 队列列表只渲染需要展示的前 N 条，不一次性渲染全部历史。
- 主题变量使用 CSS 变量更新，避免整页重绘。
- 图片、复杂阴影和大面积动画尽量少用，保证直播画面合成稳定。

可靠性要求：

- 弹幕连接断开后自动重连。
- 本地 WebSocket 断开后前端自动重连。
- 前端重连后主动拉取一次当前队列状态，避免漏消息。
- 后端出现端口冲突时自动切换备用端口并提示主播。
- 软件异常退出后，下次启动可以恢复未完成队列。

建议性能目标：

- 从后端识别点歌到 overlay 更新，正常情况下目标低于 1 秒。
- 普通直播间弹幕量下，CPU 占用应保持很低。
- 歌曲库几千首时，搜索、分类筛选、首字母展示应保持流畅。
- 连续直播数小时后，内存不应持续无上限增长。

### 4.5 本地访问边界

默认只监听：

```text
127.0.0.1
```

默认不开放局域网和公网访问，避免外部人员访问主播管理页。

## 5. 推荐技术路线

第一版推荐：

```text
Go 本地后端 + SQLite + 嵌入式静态前端
```

原因：

- Go 可以打包成单个 Windows `.exe`。
- 资源占用比 Electron 更低。
- 适合长期保持 WebSocket 连接。
- SQLite、Excel 解析、WebSocket、HTTP 服务都有成熟库。
- 前端可以构建成静态文件后嵌入后端。

前端建议：

```text
Vue 或 React + TypeScript + CSS 变量主题系统
```

后端建议：

```text
Go HTTP 服务 + WebSocket + SQLite + Excel 导入
```

本地地址：

```text
http://127.0.0.1:3000/admin
http://127.0.0.1:3000/overlay/queue
http://127.0.0.1:3000/overlay/songs
```

## 6. 总体数据流

```text
Bilibili 直播间弹幕
    ↓
本地后端弹幕监听
    ↓
点歌指令解析
    ↓
歌曲库匹配和队列规则判断
    ↓
更新内存队列状态
    ↓
本地 WebSocket 推送
    ↓
直播画面 overlay 刷新
    ↓
SQLite 短事务持久化
    ↓
观众在直播画面看到点歌板变化
```

## 7. 工程阶段

本项目必须按重要度和实现梯度推进，不能跳过核心链路先做复杂 UI 或扩展功能。

优先级分层：


| 优先级 | 阶段                          | 目的                                        | 是否第一版必须完成     |
| -------- | ------------------------------- | --------------------------------------------- | ------------------------ |
| P0     | 阶段 0：技术验证              | 确认 Bilibili 弹幕监听和直播姬 overlay 可行 | 必须                   |
| P0     | 阶段 1：最小可用版 MVP        | 打通弹幕点歌到直播画面展示的闭环            | 必须                   |
| P0     | 阶段 2：歌曲库和 Excel 导入   | 建立主播本地歌库、分类和导入能力            | 必须                   |
| P1     | 阶段 3：直播歌曲展示板        | 在直播画面滚动展示可点歌单                  | 必须                   |
| P1     | 阶段 4：随机点歌和分类点歌    | 从本地歌库随机点歌，支持按分类随机          | 必须                   |
| P1     | 阶段 5：主播操作体验          | 完善置顶、复制、删除、切歌等直播中操作      | 必须                   |
| P2     | 阶段 6：UI 主题和傻瓜式自定义 | 提供女生主播友好的主题和颜色调整            | 第一版建议完成         |
| P2     | 阶段 7：稳定性和打包          | 打包 exe、备份、重连、日志和恢复            | 第一版必须完成基础能力 |
| P3     | 阶段 8：后续扩展              | 黑名单、标签随机、全民 K 歌深度接入等       | 后续迭代               |

开发原则：

- 先验证最不确定的 Bilibili 弹幕接入和直播姬 overlay。
- 再做点歌闭环，确保观众点歌后能快速出现在直播画面。
- 再做本地歌曲库、Excel 导入和分类。
- 再做歌曲展示板和随机点歌。
- 再优化主播管理体验和 UI 主题。
- 最后处理打包、备份、重连和高级扩展。

### 阶段 0：技术验证

目的：确认核心链路能跑通，降低后续风险。

要做：

- 验证 Bilibili 弹幕监听方式。
- 验证能拿到弹幕内容、用户昵称、用户标识。
- 验证 `点歌 歌名` 和 `点歌歌名` 能被解析。
- 验证本地 overlay 能被浏览器打开。
- 验证直播姬能加载本地 overlay。

验收标准：

- 观众发弹幕后，本地程序能打印出弹幕内容和用户信息。
- 本地 overlay 页面能显示一条模拟点歌。
- 直播姬能把 overlay 显示到直播画面中。

### 阶段 1：最小可用版 MVP

目的：先完成弹幕点歌到直播画面展示的闭环。

要做：

- 本地后端启动。
- 配置直播间号。
- 监听 Bilibili 直播间弹幕。
- 识别 `点歌 歌名` 和 `点歌歌名`。
- 创建 SQLite 数据库。
- 保存点歌记录。
- 维护点歌队列。
- 提供点歌队列 overlay。
- 提供基础主播管理页。

前端包含：

- 点歌队列板。
- 当前点歌显示。
- 等待队列显示。
- 暂停点歌状态。
- 基础管理页。

后端包含：

- 弹幕监听。
- 指令解析。
- 队列新增、删除、切歌、清空。
- 本地 WebSocket 推送。
- SQLite 初始化。

验收标准：

- 观众发 `点歌 晴天` 后，直播画面点歌板显示 `晴天` 和点歌人。
- 主播可以删除、切歌、清空队列。
- 重启软件后，基础配置仍然存在。

### 阶段 2：歌曲库和 Excel 导入

目的：让主播拥有自己的本地可点歌库，并支持分类。

要做：

- 设计 `songs`、`song_categories`、`song_tags` 表。
- 支持导入 Excel。
- Excel 字段支持 `歌曲名字`、`类别`、`歌手`。
- 自动创建不存在的分类。
- 生成歌曲拼音和首字母。
- 支持重复歌曲判断。
- 支持歌曲库搜索、筛选、编辑、删除。

前端包含：

- 歌曲库管理页。
- Excel 导入页面。
- 导入结果页面。
- 分类筛选。
- 歌名和歌手搜索。

后端包含：

- Excel 解析。
- 数据校验。
- 分类自动创建。
- 重复歌曲处理。
- 拼音首字母索引。
- 歌曲库 CRUD 接口。

验收标准：

- 主播导入 Excel 后，歌曲进入 SQLite 数据库。
- 歌曲能按类别分组。
- 歌曲能按首字母排序。
- 重复歌曲不会无限重复写入。

### 阶段 3：直播歌曲展示板

目的：让观众在直播画面里看到主播可点歌单。

要做：

- 提供歌曲展示板 overlay。
- 从本地数据库读取歌曲。
- 按首字母排序展示。
- 支持按分类展示。
- 支持自动滚动。
- 支持滚动速度设置。
- 支持展示歌曲名、歌手、类别。

前端包含：

- 歌曲展示板。
- 分类标题。
- 首字母分组。
- 自动滚动列表。
- 空歌库提示。

后端包含：

- 歌曲展示查询接口。
- 分类查询接口。
- overlay 设置接口。

验收标准：

- 直播姬加载歌曲展示板后，直播画面可以滚动展示本地歌库。
- 歌曲按首字母排序。
- 切换分类后，只展示该分类歌曲。

### 阶段 4：随机点歌和分类点歌

目的：让观众可以从主播本地会唱歌库里随机点歌，并支持按分类随机。

要做：

- 支持 `随机点歌`。
- 支持 `随机 分类名`，例如 `随机 古风`、`随机 粤语`。
- 支持只从 `is_enabled = true` 的歌曲里随机。
- 支持按分类筛选随机范围。
- 支持避免短时间重复随机同一首歌。
- 支持随机结果加入点歌队列。
- 支持 overlay 显示随机点歌结果。
- 支持管理页设置哪些分类允许随机。

前端包含：

- 随机点歌设置。
- 分类随机开关。
- 随机结果提示样式。
- 队列中标识点歌来源为 `随机`。

后端包含：

- 随机点歌指令解析。
- 分类查询。
- 随机选择算法。
- 重复控制。
- 随机结果入队。
- 随机点歌历史记录。

验收标准：

- 观众发送 `随机点歌` 后，系统能从本地可点歌库随机选择一首加入队列。
- 观众发送 `随机 古风` 后，系统只从古风分类里随机。
- 随机结果能在点歌队列板里显示。

### 阶段 5：主播操作体验

目的：让主播直播中操作足够快、足够简单。

要做：

- 点歌置顶。
- 复制歌名。
- 删除点歌。
- 标记已唱。
- 跳过歌曲。
- 手动添加点歌。
- 暂停/恢复接收点歌。
- 用户冷却时间。
- 是否只允许点本地歌库歌曲。
- 队列最大长度。

前端包含：

- 大按钮模式。
- 当前点歌突出显示。
- 每首点歌旁边的快捷操作。
- 操作成功轻提示。
- 危险操作二次确认。

后端包含：

- 队列状态管理。
- 冷却规则。
- 歌库匹配规则。
- 操作日志。

验收标准：

- 主播可以一键置顶、复制、删除、切歌。
- 误点清空队列前有确认。
- 冷却和队列上限能生效。

### 阶段 6：UI 主题和傻瓜式自定义

目的：让女生主播能轻松调出适合自己直播间的风格。

要做：

- 提供 3 到 5 套主题预设。
- 支持主色、辅助色、文字色调整。
- 支持背景透明度调整。
- 支持字号、圆角、边框调整。
- 支持新点歌提示动画开关。
- 支持歌曲展示板滚动速度调整。
- 支持实时预览。
- 支持一键恢复默认样式。

推荐主题：

- 粉色甜系。
- 蓝白清爽。
- 薄荷清新。
- 深色夜间。
- 简约黑白。

控件要求：

- 颜色使用色块选择器。
- 透明度使用滑杆。
- 字号使用加减按钮或数字输入。
- 主题使用卡片预设。
- 不要求主播写 CSS。

验收标准：

- 主播可以不用改代码、不写配置，直接调颜色和样式。
- 调整后 overlay 实时预览。
- 保存后直播姬里的画面样式同步变化。

### 阶段 7：稳定性和打包

目的：让普通主播可以长期使用。

要做：

- 打包 Windows `.exe`。
- 首次启动自动建库。
- 端口冲突自动换端口。
- 弹幕断线自动重连。
- 数据库自动备份。
- 数据库备份恢复。
- 日志大小控制。
- 软件异常关闭后恢复队列。
- 提供 Excel 导入模板下载。
- 支持导出歌曲库 Excel。

验收标准：

- 主播双击 `.exe` 即可启动。
- 数据库损坏或误删时有备份可恢复。
- 长时间直播时软件不明显涨内存。
- 断线后能自动重连并提示状态。

### 阶段 8：后续扩展

目的：在核心稳定后增加高级功能。

可选功能：

- 按标签随机点歌。
- 用户黑名单。
- 歌曲黑名单。
- 房管远程管理。
- 多直播间配置。
- 全民 K 歌深度接入。
- 付费点歌。
- 数据统计和排行榜。

注意：远程管理、多直播间同步、付费点歌、云端数据统计会改变“完全本地无服务器”的约束，需要单独评估。

## 8. 前端详细设计

### 8.1 前端界面列表

前端分为：

- 点歌队列板：直播画面 overlay，给观众看。
- 歌曲展示板：直播画面 overlay，给观众看。
- 主播管理页：主播本地操作后台。
- 歌曲库管理页：主播管理歌曲、分类、导入 Excel。
- 设置页：配置直播间、点歌规则、UI 主题。

### 8.2 点歌队列板

地址：

```text
http://127.0.0.1:3000/overlay/queue
```

显示内容：

- 当前点歌。
- 点歌人昵称。
- 等待队列。
- 最新点歌提示。
- 暂停点歌状态。
- 空队列状态。

显示要求：

- 默认透明背景。
- 当前点歌比队列更醒目。
- 置顶歌曲有明显标识。
- 队列过长时只展示前 N 首。
- 新点歌可以短暂高亮。
- 不做高频动画。

### 8.3 歌曲展示板

地址：

```text
http://127.0.0.1:3000/overlay/songs
```

显示内容：

- 歌曲名。
- 歌手。
- 分类。
- 首字母分组。

展示规则：

- 默认按拼音首字母排序。
- 支持按分类筛选。
- 支持自动滚动。
- 滚动到末尾后循环。
- 支持设置滚动速度。

### 8.4 主播管理页

地址：

```text
http://127.0.0.1:3000/admin
```

队列操作：

- 置顶。
- 复制歌名。
- 删除。
- 标记已唱。
- 跳过。
- 切歌。
- 清空队列。
- 手动添加点歌。
- 暂停/恢复点歌。

管理页 UI 要求：

- 分区清楚：连接状态、当前点歌、等待队列、歌曲库、设置。
- 当前点歌放在最醒目的位置。
- 常用按钮足够大，方便直播中点击。
- 危险操作二次确认。
- 操作结果使用轻提示。

### 8.5 歌曲库管理页

功能：

- 查看全部歌曲。
- 按首字母排序。
- 按分类筛选。
- 按歌名搜索。
- 按歌手搜索。
- 新增歌曲。
- 编辑歌曲。
- 删除歌曲。
- 设置是否可点。
- 导入 Excel。
- 导出 Excel。
- 下载 Excel 模板。

列表字段：

```text
歌曲名 | 歌手 | 分类 | 标签 | 是否可点 | 更新时间
```

### 8.6 主题编辑器

目标：傻瓜式调整，不要求主播懂技术。

功能：

- 主题预设。
- 主色。
- 辅助色。
- 文字色。
- 背景色。
- 背景透明度。
- 圆角。
- 字号。
- 边框。
- 动画开关。
- 实时预览。
- 恢复默认。

## 9. 后端详细设计

### 9.1 本地服务

本地服务默认地址：

```text
http://127.0.0.1:3000
```

路由：

```text
/admin
/settings
/songs
/overlay/queue
/overlay/songs
/api/*
/ws
```

### 9.2 弹幕监听

功能：

- 连接指定 Bilibili 直播间。
- 接收弹幕消息。
- 提取弹幕内容。
- 提取用户昵称。
- 提取用户标识。
- 断线自动重连。
- 推送连接状态给管理页。

### 9.3 指令解析

第一版支持：

```text
点歌 歌名
点歌歌名
```

规则：

- 必须以 `点歌` 开头。
- `点歌` 后可有空格，也可没有空格。
- 后面的内容作为歌名。
- 歌名为空则忽略。

后续扩展：

```text
随机点歌
随机 古风
歌单
分类
```

### 9.4 队列管理

功能：

- 新增点歌。
- 获取当前点歌。
- 获取等待队列。
- 置顶点歌。
- 删除点歌。
- 标记已唱。
- 跳过歌曲。
- 切歌。
- 清空队列。
- 暂停/恢复点歌。
- 用户冷却。
- 队列上限。
- 本地歌库匹配。
- 内存维护当前队列状态，保证前端更新快。
- SQLite 持久化使用短事务，避免阻塞弹幕处理。
- 提供队列状态快照接口，前端重连后可重新同步。
- 对短时间重复点歌和刷屏点歌做限流。

### 9.5 Excel 导入

Excel 支持字段：

```text
歌曲名字
类别
歌手
```

导入规则：

- `歌曲名字` 必填。
- `类别` 为空则归入默认分类。
- `歌手` 可为空。
- 分类不存在时自动创建。
- 同名同歌手默认视为重复。
- 同名不同歌手可作为不同歌曲。
- 导入全过程本地完成，不上传文件。

导入结果：

```text
总行数
成功数量
重复数量
失败数量
新增分类数量
失败原因
```

### 9.6 设置和运维

设置项：

- 直播间号。
- 点歌关键词。
- 是否允许无空格点歌。
- 是否只允许点本地歌库。
- 是否允许重复点歌。
- 用户冷却时间。
- 队列最大长度。
- overlay 显示数量。
- 歌曲展示默认分类。
- 滚动速度。
- 主题配置。

运维能力：

- 数据库自动初始化。
- 数据库版本迁移。
- 数据库自动备份。
- 备份恢复。
- 日志轮转。
- 端口冲突检测。
- 弹幕重连。
- 导入模板下载。
- 歌曲库导出。
- 后台任务队列。
- WebSocket 断线重连。
- overlay 状态快照恢复。
- 长时间运行内存监控。
- 弹幕消息处理限流。

## 10. 数据库设计

### 10.1 核心表

```text
songs                歌曲库
song_categories      歌曲分类
song_tags            歌曲标签
song_tag_links       歌曲标签关系
queue                当前点歌队列
requests             点歌历史
settings             本地设置
import_batches       导入批次
blocked_users        用户黑名单
blocked_songs        歌曲黑名单
backups              备份记录
```

### 10.2 songs 表

```text
id                  本地歌曲 ID
name                歌曲名称
name_pinyin         歌曲拼音
name_initial        首字母
artist              歌手
category_id         分类 ID
is_enabled          是否可点
note                备注
created_at          创建时间
updated_at          更新时间
```

### 10.3 song_categories 表

```text
id                  分类 ID
name                分类名称
sort_order          排序
is_enabled          是否启用
created_at          创建时间
updated_at          更新时间
```

### 10.4 queue 表

```text
id                  队列 ID
song_id             关联歌曲 ID，可为空
song_name           歌曲名称
artist              歌手
category_name       分类
requester_uid       点歌用户标识
requester_name      点歌用户昵称
source              danmaku / admin / random
status              waiting / current / done / skipped / deleted
is_pinned           是否置顶
created_at          点歌时间
updated_at          更新时间
```

## 11. 可能遗漏但建议补充的功能

建议补充：

- 导入前预览：避免错误 Excel 一次性导入大量脏数据。
- 重复歌曲合并：同一首歌导入多次时可合并。
- 数据备份恢复：避免主播误删歌库。
- 日志查看：管理页能看到最近错误。
- 端口冲突提示：`3000` 被占用时自动切换并显示新地址。
- 连接状态提示：让主播知道弹幕监听是否正常。
- 空状态设计：没有歌曲、没有点歌、断线时都有明确提示。
- 导出歌库：主播可以备份成 Excel。
- 本地迁移说明：换电脑时怎么迁移数据库。
- 黑名单预留：后续处理刷屏用户或禁点歌曲。
- 性能限制：限制 overlay 动画和刷新频率。

暂不建议第一版做：

- 自动播放音乐。
- 自动接全民 K 歌。
- 远程房管管理。
- 付费点歌。
- 多主播云同步。
- 复杂账号系统。

这些会显著增加复杂度或破坏“无服务器、本地低消耗”的核心约束。

## 12. 已确认需求追踪清单

以下是前面讨论过的要求，文档和工程计划中都需要覆盖。

### 12.1 直播和弹幕

- Bilibili 直播间观众发送弹幕点歌。
- 支持 `点歌 歌名`。
- 支持 `点歌歌名`。
- 主播电脑本地后端监听当前直播间弹幕。
- 后端捕捉弹幕里的用户昵称、用户标识、歌名和发送时间。
- 弹幕监听优先评估官方开放平台，必要时评估公开 WebSocket 方案。

### 12.2 无服务器和本地数据

- 不使用自建服务器。
- 不使用云数据库。
- 不使用远程后端中转。
- 不产生固定月费。
- 谁直播，数据库就在谁电脑里。
- 数据库使用本地 SQLite。
- 默认只监听 `127.0.0.1`。
- 不开放公网管理页。

### 12.3 直播画面插件

- 插件用于直播画面展示。
- 通过直播姬 H5 插件、网页源或 overlay 加载。
- 观众在直播画面中看到点歌板。
- 点歌板不是 Bilibili 网页弹幕区里的组件。
- 支持点歌队列板。
- 支持歌曲展示板。

### 12.4 前端 UI

- 前端分为点歌队列板、歌曲展示板、主播管理页、歌曲库管理页、设置页。
- 歌曲展示板滚动展示数据库里的歌曲。
- 歌曲展示按首字母/拼音排序。
- 歌曲展示支持按分类筛选。
- 点歌队列板展示当前点歌和等待队列。
- 每首点歌支持置顶、复制歌名、删除、标记已唱、跳过。
- UI 适合女生主播使用。
- 颜色支持傻瓜式调整。
- 支持主题预设。
- 支持色块、滑杆、实时预览。
- 不要求主播写 CSS 或配置文件。

### 12.5 后端和数据库

- 支持 Excel 导入歌曲。
- Excel 字段支持 `歌曲名字`、`类别`、`歌手`。
- 导入后转入 SQLite 数据库。
- 分类不存在时自动创建。
- 支持歌曲库、分类、标签、队列、历史、设置、导入批次、备份记录。
- 支持导出歌曲库 Excel。
- 支持下载 Excel 模板。
- 支持数据库自动备份和恢复。

### 12.6 随机点歌

- 支持从主播会唱歌库里随机点歌。
- 支持按分类随机点歌，例如 `随机 古风`。
- 随机结果加入当前点歌队列。
- 随机点歌仍然只使用主播电脑本地数据库。

### 12.7 低消耗

- 后端保持轻量长连接。
- 不高频轮询。
- 不高频写数据库。
- overlay 数据变化时刷新。
- 使用内存队列保证 UI 更新快。
- 使用 SQLite 短事务持久化。
- WebSocket 使用状态快照和重连机制。
- Excel 导入等重任务后台执行，不阻塞点歌。
- 第一版不自动播放、不下载、不转码音乐。
- 日志控制大小。
- 打包后尽量低内存、低 CPU 占用。

### 12.8 第三方音乐平台边界

- 可以把全民 K 歌作为主播手动搜索和演唱的平台。
- 可以维护“全民 K 歌可唱歌单”。
- 第一版不自动控制全民 K 歌。
- 第一版不自动搜索全民 K 歌曲库。
- 第一版不自动拉取伴奏或播放音乐。

## 13. 第一版最终验收标准

第一版完成后必须满足：

- 主播双击软件即可本地启动。
- 主播能配置 Bilibili 直播间号。
- 软件能监听当前直播间弹幕。
- 观众发送 `点歌 晴天` 后，软件能识别并加入队列。
- 点歌队列板能在直播姬画面里显示当前点歌和等待队列。
- 歌曲展示板能在直播姬画面里滚动展示本地歌库。
- 歌曲展示支持按首字母排序。
- 歌曲展示支持按分类筛选。
- 主播可以导入 Excel 歌单。
- Excel 字段支持 `歌曲名字`、`类别`、`歌手`。
- 导入后歌曲进入本地 SQLite 数据库。
- 观众可以发送 `随机点歌` 从主播本地歌库随机点一首。
- 观众可以发送 `随机 分类名` 从指定分类随机点歌。
- 主播可以置顶、复制歌名、删除、切歌、清空队列。
- 主播可以使用主题预设和色块调整 UI。
- 主播不需要写 CSS 或配置文件。
- 数据只保存在主播电脑本地。
- 系统不使用自建服务器。
- 系统不产生固定月费。
- 系统低消耗运行，不明显影响直播。
- 普通弹幕量下，从识别点歌到 overlay 更新目标低于 1 秒。
- 弹幕连接断开后能自动重连，并在管理页显示状态。
- overlay WebSocket 断开后能自动重连，并重新同步当前队列。
- Excel 导入大量歌曲时，不应阻塞已有点歌队列展示。
- 连续直播数小时后，内存不应持续无上限增长。

## 14. 风险和边界

### 14.1 Bilibili 接入风险

Bilibili 弹幕接入能力可能随平台规则变化。正式实现前必须先做阶段 0 技术验证。

如果走官方开放平台，可能需要：

- 开发者身份认证。
- 应用创建。
- 权限申请。
- 主播授权。

如果走非官方公开 WebSocket，可能存在：

- 协议变化。
- 被限制连接。
- 稳定性不可控。
- 合规风险。

### 14.2 直播姬加载风险

overlay 本质是本地网页。只要直播姬支持 H5 插件或网页源，就可以显示。如果某个版本直播姬限制本地地址，需要用 OBS 浏览器源或其他兼容方式验证。

### 14.3 全民 K 歌风险

第一版只把全民 K 歌当作主播手动搜索和演唱的平台，不自动控制。深度接入需要单独评估官方接口、版权和授权。

## 15. 推荐开发顺序

严格按下面顺序做：

1. 阶段 0：先验证 Bilibili 弹幕监听和直播姬 overlay。
2. 阶段 1：做点歌 MVP，打通弹幕到点歌板。
3. 阶段 2：做歌曲库、分类和 Excel 导入。
4. 阶段 3：做歌曲展示板。
5. 阶段 4：做随机点歌和分类随机点歌。
6. 阶段 5：完善主播管理操作。
7. 阶段 6：做女生主播友好的主题和傻瓜式 UI 调整。
8. 阶段 7：打包、备份、重连、日志和稳定性。
9. 阶段 8：再考虑黑名单、全民 K 歌等扩展。

这个顺序能保证先验证最不确定的 Bilibili 接入和直播画面展示，再逐步增加数据库、UI 和高级功能，避免一开始做大量界面后发现核心链路有问题。
